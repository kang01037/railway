<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.railway.order.mapper.OrderMapper">

    <resultMap id="BaseResultMap" type="com.railway.order.entity.OrderDO">
        <id     column="id"          property="id"/>
        <result column="order_no"    property="orderNo"/>
        <result column="user_id"     property="userId"/>
        <result column="train_no"    property="trainNo"/>
        <result column="run_date"    property="runDate"/>
        <result column="seat_type"   property="seatType"/>
        <result column="num"         property="num"/>
        <result column="amount"      property="amount"/>
        <result column="status"      property="status"/>
        <result column="expire_time" property="expireTime"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, order_no, user_id, train_no, run_date, seat_type,
        num, amount, status, expire_time, create_time, update_time
    </sql>

    <insert id="insert" parameterType="com.railway.order.entity.OrderDO">
        INSERT INTO orders (
            id, order_no, user_id, train_no, run_date, seat_type,
            num, amount, status, expire_time, create_time, update_time
        ) VALUES (
            #{id}, #{orderNo}, #{userId}, #{trainNo}, #{runDate}, #{seatType},
            #{num}, #{amount}, #{status}, #{expireTime}, #{createTime}, #{updateTime}
        )
    </insert>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM orders WHERE id = #{id}
    </select>

    <select id="selectByOrderNo" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM orders WHERE order_no = #{orderNo} LIMIT 1
    </select>

    <select id="listByUserId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM orders
        WHERE user_id = #{userId}
        ORDER BY create_time DESC
    </select>

    <select id="countByUserId" resultType="int">
        SELECT COUNT(*) FROM orders WHERE user_id = #{userId}
    </select>

    <!-- 状态机：0 → 1（已支付） -->
    <update id="confirmByOrderNo">
        UPDATE orders SET status = 1
        WHERE order_no = #{orderNo} AND status = 0
    </update>

    <!-- 状态机：0 → 2（用户取消，仅待支付） -->
    <update id="cancelByOrderNo">
        UPDATE orders SET status = 2
        WHERE order_no = #{orderNo} AND status = 0
    </update>

    <!-- 状态机：0/1 → 3（退款/关单）。关单消费者会用到 -->
    <update id="refundByOrderNo">
        UPDATE orders SET status = 3
        WHERE order_no = #{orderNo} AND status IN (0, 1)
    </update>

    <!-- 状态机：1 → 4（已完成） -->
    <update id="completeByOrderNo">
        UPDATE orders SET status = 4
        WHERE order_no = #{orderNo} AND status = 1
    </update>

    <!-- 出票失败补偿：1 → 0（回滚到待支付） -->
    <update id="rollbackConfirm">
        UPDATE orders SET status = 0
        WHERE order_no = #{orderNo} AND status = 1
    </update>

</mapper>
