server:
  port: 9102

spring:
  application:
    name: service-train-stock
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      # demo 闃舵閰嶇疆涓績鍏抽棴
      config:
        enabled: false
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/railway-real?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: 127.0.0.1
      port: 6379
      timeout: 2s

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.railway.trainstock.entity
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.slf4j.Slf4jImpl

pagehelper:
  helper-dialect: mysql
  reasonable: true
  support-methods-arguments: true

# JWT锛堜笌 service-user 涓€鑷达級
jwt:
  secret: 0123456789abcdef0123456789abcdef0123456789abcdef
  expire-minutes: 120
  issuer: railway

# 闆姳 worker-id锛氭湰鏈嶅姟 2锛坓ateway=0, user=1, train-stock=2, ticket=3, payment=4, order=5, notification=6, search=7锛?snowflake:
  worker-id: 2

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: when_authorized

logging:
  level:
    com.railway: debug
    com.railway.trainstock.mapper: debug
