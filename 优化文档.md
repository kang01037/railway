# 项目优化文档

## 一、优化任务清单

| 优先级 | 任务 | 说明 | 状态 |
|---|---|---|---|
| P1 | 搜索批量余票接口 | 当前逐个车次 Feign 查余票（N+1 问题），改为批量接口一次调用 | 🔄 未完成 |
| P1 | 分布式事务一致性 | 订单创建跨 Redis+MySQL 无事务保障，stock 成功但 ticket 失败会不一致 | 🔄 未完成 |
| P1 | 支付回调幂等性 | 支付回调重复发送可能导致订单重复处理，需加去重 | 🔄 未完成 |
| P2 | 座位分配策略 | 当前 SPOP 随机分配座位，用户无法选座 | 🔄 未完成 |
| P2 | 动态库存锁定时间 | 当前固定 15 分钟锁定，热门车次应缩短锁定时间 | 🔄 未完成 |
| P2 | 搜索 Feign 超时与熔断 | 搜索接口对每个车次调 Feign，超时影响整体响应 | 🔄 未完成 |
| P3 | 数据库索引优化 | 确认 orders/tickets/train_seat_stock 表索引是否完善 | 🔄 未完成 |
| P3 | 业务监控告警 | 缺少购票成功率、MQ 积压、Redis 命中率等业务指标 | 🔄 未完成 |

---

## 二、各任务详细方案

### P1: 搜索批量余票接口

**现状**：搜索 N 个车次 → 逐个 Feign 调用 `/trains/{trainNo}/seats` → N 次远程调用，响应慢。

**优化方案**：
1. service-train-stock 新增批量接口：`/seats/batch?trainNos=G1234,G5678&runDate=2026-06-24`
2. 一次 Feign 调用返回所有车次的余票数据
3. service-search 改用批量接口，减少远程调用次数

**涉及模块**：
- `service-train-stock`：SeatController 新增批量接口
- `service-search`：SearchServiceImpl 改用批量调用

---

### P1: 分布式事务一致性

**现状**：订单创建涉及 3 个操作（写订单、扣库存、锁座位），跨 Redis + MySQL，没有分布式事务保障。如果 `stock.occupy` 成功但 `ticket.preAllocate` 失败，库存扣了但票没锁。

**优化方案**：
1. 本地消息表 + 最终一致性
   - 每个操作先写本地消息表
   - 定时任务扫描未完成的消息进行补偿
2. 或 Saga 模式
   - 每一步都有补偿操作（stock.release、ticket.release）
   - 失败时按逆序补偿

**涉及模块**：
- `service-order`：OrderCreateConsumer 加事务边界和补偿逻辑
- `common`：新增本地消息表或 Saga 框架

---

### P1: 支付回调幂等性

**现状**：支付回调如果重复发送，可能导致订单重复处理（重复扣库存、重复出票）。

**优化方案**：
1. OrderPaidConsumer 加幂等检查
   - 用 orderNo 做去重（Redis SETNX 或数据库唯一约束）
   - 检查订单当前状态，只有 status=0 才处理
2. 支付流水表加唯一约束（transactionNo）

**涉及模块**：
- `service-order`：OrderPaidConsumer 加幂等逻辑
- `service-payment`：支付流水表加唯一索引

---

### P2: 座位分配策略

**现状**：当前 `SPOP` 随机分配座位，用户无法选择具体座位号。

**优化方案**：
1. 支持用户指定座位号（选座功能）
2. 或按车厢/座位偏好分配（靠窗、靠过道、静音车厢）
3. 座位池从 Set 改为 Sorted Set（按座位号排序）

**涉及模块**：
- `service-ticket`：TicketServiceImpl 支持指定座位
- `service-train-stock`：座位池数据结构改为 ZSet

---

### P2: 动态库存锁定时间

**现状**：当前固定锁定 15 分钟，热门车次可能被大量锁座导致其他人买不到。

**优化方案**：
1. 动态锁定时间
   - 普通车次：15 分钟
   - 热门车次（余票 < 20%）：5 分钟
   - 秒杀车次：2 分钟
2. 或分阶段锁定
   - 先锁 2 分钟确认意向
   - 再锁 10 分钟支付

**涉及模块**：
- `service-order`：OrderServiceImpl 动态计算锁定时间
- `service-train-stock`：StockServiceImpl 支持自定义锁定时间

---

### P2: 搜索 Feign 超时与熔断

**现状**：搜索接口对每个车次都调 Feign，如果某个车次超时会影响整体响应。

**优化方案**：
1. 批量接口减少调用次数（配合 P1 搜索批量接口）
2. 加熔断降级（Resilience4j）
   - 单个车次查询失败不影响其他车次
   - 降级返回"余票信息暂不可用"

**涉及模块**：
- `service-search`：SearchServiceImpl 加熔断配置
- `service-search`：pom.xml 加 Resilience4j 依赖

---

### P3: 数据库索引优化

**现状**：需要确认以下索引是否存在，缺失会导致慢查询。

**优化方案**：
```sql
-- orders 表
CREATE INDEX idx_orders_user_id ON orders(user_id);
CREATE INDEX idx_orders_status ON orders(status);
CREATE INDEX idx_orders_create_time ON orders(create_time);

-- train_seat_stock 表
CREATE INDEX idx_stock_train_date ON train_seat_stock(train_no, run_date);
CREATE INDEX idx_stock_seat_type ON train_seat_stock(seat_type);

-- tickets 表
CREATE INDEX idx_tickets_order_no ON tickets(order_no);
CREATE INDEX idx_tickets_user ON tickets(user_id);
CREATE INDEX idx_tickets_train_date ON tickets(train_no, run_date);
```

**涉及模块**：
- `db`：新增索引 SQL 脚本

---

### P3: 业务监控告警

**现状**：当前只有 actuator health，缺少业务指标监控。

**优化方案**：
1. 关键业务指标
   - 购票成功率/失败率
   - 平均响应时间（P50/P95/P99）
   - MQ 消息积压量
   - Redis 命中率
   - 库存扣减失败率
2. 告警规则
   - 购票成功率 < 95% → 告警
   - MQ 积压 > 1000 → 告警
   - 响应时间 P99 > 1s → 告警

**涉及模块**：
- `common`：新增 MetricsCollector
- 各服务：关键业务点埋点

---

## 三、优化后完整流程

```
1. 前端请求 → Gateway（限流 100 QPS/用户）
2. Gateway 校验 JWT → 路由到 Order 服务
3. Order 服务校验参数 → MQ 发送订单请求（削峰）
4. 返回"排队中" → 前端轮询
5. 消费者处理：
   ├── Redis Lua 扣库存（预热后命中率 100%）
   ├── Redis SPOP 座位号
   ├── 写 MySQL ticket(status=0) + orders(status=4排队中)
   └── 更新订单状态为"待支付"
6. 前端轮询到"待支付" → 返回支付链接
7. 用户支付 → payment.callback()
   ├── 记录流水（幂等检查）
   ├── MQ 发送支付成功消息（异步化）
   └── 返回"支付成功"
8. 消费者异步处理：
   ├── order 0→1
   ├── ticket 0→1
   └── stock.confirm()
9. 15min 未支付 → MQ 延迟队列关单
```
