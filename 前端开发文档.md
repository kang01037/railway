# Railway-Real 前端开发文档（AI专用）

## 架构概览

- 网关: localhost:9000, 所有请求走 `/api/{service}/**`
- 鉴权: JWT Bearer Token, Header: `Authorization: Bearer {token}`
- 统一返回体: `{"code":200,"message":"成功","data":T,"traceId":"xxx"}`
- 判断成功: `code === 200`
- 日期格式: `yyyy-MM-dd`, 日期时间格式: `yyyy-MM-dd HH:mm:ss`

## 网关路由映射

| 前端请求前缀 | 目标服务 | StripPrefix |
|---|---|---|
| `/api/user/**` | service-user | 2 |
| `/api/search/**` | service-search | 2 |
| `/api/ticket/**` | service-ticket | 2 |
| `/api/order/**` | service-order | 2 |
| `/api/payment/**` | service-payment | 2 |
| `/api/train/**` | service-train-stock | 2 |
| `/api/notification/**` | service-notification | 2 |

StripPrefix=2 意味着 `/api/user/auth/login` → 服务收到 `/auth/login`

## 鉴权白名单（无需Token）

- `POST /api/user/auth/login`
- `POST /api/user/auth/register`
- `POST /api/payment/pay/callback/**`
- 其余所有接口均需 `Authorization: Bearer {token}`

## 错误码

| code | 含义 |
|---|---|
| 200 | 成功 |
| 400 | 参数错误 |
| 401 | 未登录/Token过期 |
| 403 | 无权限 |
| 404 | 资源不存在 |
| 409 | 资源冲突 |
| 500 | 服务器错误 |
| 1001 | 重复请求(幂等) |
| 1101 | 用户不存在 |
| 1102 | 用户名或密码错误 |
| 1103 | 用户已存在 |
| 1201 | 乘车人不存在 |
| 1301 | 车次不存在 |
| 2001 | 库存不足 |
| 3001 | 订单不存在 |
| 3003 | 订单已支付 |
| 3004 | 订单取消失败 |
| 3005 | 订单创建失败 |
| 4001 | 支付失败 |
| 4002 | 支付单不存在 |
| 5001 | 出票失败 |
| 5002 | 票不存在 |

---

## API 详细说明

### 1. 认证模块 `/api/user/auth`

#### POST /api/user/auth/register
- 无需Token
- Request:
```json
{
  "username": "string(3-50,必填)",
  "password": "string(6-50,必填)",
  "phone": "string(可选,1[3-9]开头的11位手机号)",
  "email": "string(可选,合法邮箱)",
  "realName": "string(可选,max50)"
}
```
- Response: `data: Long` (userId)

#### POST /api/user/auth/login
- 无需Token
- Request:
```json
{
  "username": "string(必填)",
  "password": "string(必填)"
}
```
- Response:
```json
{
  "token": "jwt_token_string",
  "userId": 123,
  "username": "张三",
  "roles": ["USER"],
  "expireAt": 1718000000000
}
```
- 登录后所有请求带 `Authorization: Bearer {token}`, token有效期2小时

#### POST /api/user/auth/logout
- 需要Token
- Response: 无data

#### GET /api/user/auth/me
- 需要Token
- Response:
```json
{
  "userId": 123,
  "username": "张三",
  "roles": ["USER"]
}
```

### 2. 乘车人模块 `/api/user/passengers`
- 所有接口需要Token

#### POST /api/user/passengers
- 新增乘车人
- Request:
```json
{
  "name": "string(必填,max50)",
  "idCardNo": "string(必填,15-32位)",
  "phone": "string(可选,手机号)",
  "passengerType": 0,
  "isDefault": 0
}
```
- passengerType: 0-成人 1-儿童 2-学生
- isDefault: 0-否 1-默认乘车人
- Response: `data: Long` (passengerId)

#### GET /api/user/passengers
- 分页查询当前用户乘车人
- Query: `pageNum=1&pageSize=10&passengerType=0`
- Response: `data: PageInfo<PassengerVO>`
```json
{
  "total": 5,
  "pageNum": 1,
  "pageSize": 10,
  "pages": 1,
  "list": [{
    "id": 123,
    "userId": 1,
    "name": "张三",
    "idCardType": 0,
    "idCardNo": "360313********2031",
    "phone": "13800138000",
    "passengerType": 0,
    "isDefault": 1,
    "createTime": "2026-06-07 12:00:00"
  }]
}
```
- 注意: idCardNo已脱敏(前6+****+后4)

#### GET /api/user/passengers/{id}
- Response: `data: PassengerVO`

#### PUT /api/user/passengers/{id}
- Request: 同POST的body
- Response: 无data

#### DELETE /api/user/passengers/{id}
- Response: 无data

### 3. 车次搜索模块 `/api/search/search`
- 无需Token

#### GET /api/search/search/trains
- 搜索车次
- Query: `from=北京&to=上海&date=2026-06-10`
- from/to可选, date格式yyyy-MM-dd
- Response: `data: List<TrainIndex>`
```json
[{
  "id": "G1234",
  "trainNo": "G1234",
  "trainType": "G",
  "startStation": "北京南",
  "startStationKeyword": "北京南",
  "endStation": "上海虹桥",
  "endStationKeyword": "上海虹桥",
  "startTime": "2026-06-10T08:00:00",
  "endTime": "2026-06-10T13:30:00",
  "runDays": "1111111",
  "runDate": "2026-06-10",
  "prices": [
    {"seatType": "BUSINESS", "price": 1748.0},
    {"seatType": "FIRST", "price": 933.0},
    {"seatType": "SECOND", "price": 553.0},
    {"seatType": "STAND", "price": 553.0}
  ]
}]
```

#### GET /api/search/search/trains/{trainNo}/seats
- 查车次余票
- Query: `date=2026-06-10` (必填)
- Response: `data: List<SeatRemainVO>`
```json
[{
  "seatType": "SECOND",
  "price": 553.00,
  "total": 100,
  "remain": 85
}]
```

### 4. 订单模块 `/api/order/orders`
- 所有接口需要Token

#### POST /api/order/orders
- 创建订单, 需幂等头 `X-Idempotent-Key: {uuid}` (60秒内防重复提交)
- 购票流程: 前端发起→Redis预占座位→写订单(status=0)→返回支付链接→支付成功→写ticket表
- Request:
```json
{
  "trainNo": "G1234",
  "runDate": "2026-06-10",
  "seatType": "SECOND",
  "passengerIds": [123, 456],
  "price": 553.00
}
```
- seatType枚举: BUSINESS / FIRST / SECOND / STAND
- passengerIds: 1~5个乘车人ID
- price: 单张票价(从搜索接口获取), 总金额=price*passengerIds.length
- Response: `data: CreateOrderResultVO`
```json
{
  "orderNo": "O338925718987624448",
  "payUrl": "http://localhost:9000/api/payment/pay/callback/sim?payNo=P338925719046344704",
  "amount": 1106.00,
  "expireTime": "2026-06-07 12:38:08"
}
```
- expireTime: 支付截止时间, 15分钟后超时自动关单

#### POST /api/order/orders/{orderNo}/cancel
- 取消订单(仅status=0待支付可取消)
- Request:
```json
{
  "reason": "不想要了"
}
```
- Response: 无data

#### GET /api/order/orders/{orderNo}
- 订单详情
- Response: `data: OrderDetailVO`
```json
{
  "order": {
    "id": 123,
    "orderNo": "O338925718987624448",
    "userId": 1,
    "trainNo": "G1234",
    "runDate": "2026-06-10",
    "seatType": "SECOND",
    "num": 2,
    "amount": 1106.00,
    "status": 0,
    "expireTime": "2026-06-07 12:38:08",
    "createTime": "2026-06-07 12:23:08",
    "updateTime": "2026-06-07 12:23:08"
  },
  "tickets": []
}
```
- order.status: 0-待支付 1-已支付 2-已取消 3-已退款 4-已完成
- tickets: 当前返回空数组(后续对接ticket服务)

#### GET /api/order/orders
- 当前用户订单列表(分页)
- Query: `pageNum=1&pageSize=10`
- Response: `data: PageInfo<OrderVO>`

### 5. 支付模块 `/api/payment/pay`

#### POST /api/payment/pay/create
- 创建支付单(无需Token, 由order服务内部调用)
- Request:
```json
{
  "orderNo": "O338925718987624448",
  "amount": 1106.00,
  "payChannel": "SIM"
}
```
- payChannel: ALIPAY / WECHAT / SIM(模拟)
- Response: `data: PayVO`
```json
{
  "payNo": "P338925719046344704",
  "orderNo": "O338925718987624448",
  "amount": 1106.00,
  "payChannel": "SIM",
  "payUrl": "http://localhost:9000/api/payment/pay/callback/sim?payNo=P338925719046344704",
  "status": 0,
  "paidTime": null,
  "createTime": "2026-06-07 12:23:08"
}
```
- payVO.status: 0-待支付 1-支付成功 2-支付失败

#### POST /api/payment/pay/callback/sim
- 模拟支付回调(无需Token)
- Request:
```json
{
  "payNo": "P338925719046344704",
  "success": true
}
```
- success=true: 支付成功, 内部同步调order确认→ticket出票
- success=false: 支付失败
- Response: `data: {"payNo":"P...","processed":true}`
- processed=false表示幂等命中(已处理过)

#### GET /api/payment/pay/{payNo}
- 需要Token
- Response: `data: PayVO`

#### GET /api/payment/pay/order/{orderNo}
- 按订单号查支付记录
- 需要Token
- Response: `data: List<PayVO>`

### 6. 票务模块 `/api/ticket/tickets`

#### GET /api/ticket/tickets/order/{orderNo}
- 查订单下的票列表(需Token)
- Response: `data: List<TicketVO>`
```json
[{
  "id": 123,
  "ticketNo": "T338925719046344704",
  "orderNo": "O338925718987624448",
  "trainNo": "G1234",
  "runDate": "2026-06-10",
  "seatType": "SECOND",
  "carriageNo": 1,
  "seatNo": "5",
  "passengerId": 123,
  "passengerName": "张三",
  "idCardNo": "360313********2031",
  "price": 553.00,
  "status": 1,
  "createTime": "2026-06-07 12:25:00"
}]
```
- ticket.status: 0-待支付 1-已出票 2-已改签 3-已退
- idCardNo已脱敏

#### GET /api/ticket/tickets/{ticketNo}
- 查单张票(需Token)
- Response: `data: TicketVO`

---

## 核心业务流程

### 购票完整流程

```
1. 用户搜索车次: GET /api/search/search/trains?from=X&to=Y&date=Z
2. 查看余票: GET /api/search/search/trains/{trainNo}/seats?date=Z
3. 选择乘车人: GET /api/user/passengers (提前维护)
4. 创建订单: POST /api/order/orders (带X-Idempotent-Key头)
   → 后端: Redis SPOP预占座位 → 写orders表(status=0) → 生成支付单
   → 返回: {orderNo, payUrl, amount, expireTime}
5. 模拟支付: POST /api/payment/pay/callback/sim {payNo, success:true}
   → 后端: 同步调order确认(status 0→1) → ticket.issue(Redis预占数据→MySQL ticket表)
6. 查看订单: GET /api/order/orders/{orderNo}
7. 查看车票: GET /api/ticket/tickets/order/{orderNo}
```

### 超时关单流程

```
订单创建15分钟未支付 → MQ延迟消息触发 → order.status 0→3 → 归还Redis座位池
```

### 取消订单流程

```
POST /api/order/orders/{orderNo}/cancel → order.status 0→2 → 归还Redis座位池
```

## 前端开发注意事项

1. **Token管理**: 登录后存token, 每次请求带`Authorization: Bearer {token}`, 401时跳登录页
2. **幂等提交**: 创建订单必须带`X-Idempotent-Key`头(建议UUID), 60秒内防重复
3. **支付轮询**: 创建订单后拿到payUrl, 模拟支付后可轮询`GET /api/payment/pay/{payNo}`检查status
4. **订单状态**: 0-待支付(显示倒计时) 1-已支付 2-已取消 3-已退款 4-已完成
5. **座位类型映射**: BUSINESS→商务座 FIRST→一等座 SECOND→二等座 STAND→无座
6. **金额计算**: 前端展示总价=price(单张)*passengerIds.length, 后端也会算
7. **日期格式**: 所有日期参数用`yyyy-MM-dd`, 响应中LocalDate也是`yyyy-MM-dd`
8. **脱敏**: 乘车人idCardNo返回已脱敏, 不需要前端处理
9. **分页**: 使用PageInfo, 字段: total/pageNum/pageSize/pages/list
10. **支付链接**: payUrl是模拟支付页面, 实际场景应跳转支付宝/微信
11. **错误处理**: code!=200时展示message, 401清token跳登录, 1001提示勿重复提交
12. **搜索接口**: from/to参数可选, 不传则不过滤; date建议必填
13. **车次详情页**: 先调trains接口拿价格, 再调seats接口拿余票, 两个接口组合展示
14. **订单列表**: 按createTime倒序, 支持分页
