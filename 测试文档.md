# 铁路购票系统 - 端到端测试文档

> **最后更新**：2026-06-06
> **项目版本**：`1.0.0-SNAPSHOT`
> **栈**：Spring Boot 3.5.14 + Spring Cloud 2025.0.0 + Spring Cloud Alibaba 2025.0.0.0-preview + Java 21

本文档分三部分：
1. **测试概览** — 总成绩 + 已修 bug
2. **单元测试**（方案 C）— 60 个测试用例（已 ✅ 全部通过）
3. **E2E 联调测试**（方案 A）— 中间件启动 + 完整 curl 步骤 + 预期 + 排查（**需要中间件**）

---

## 0. 测试总成绩

| 测试套件 | 文件 | 方法数 | 状态 |
|---|---|---|---|
| common（ErrorCode / R / IdCard / Jwt / Snowflake） | 5 | 24 | ✅ |
| service-user（AuthService） | 1 | 7 | ✅ |
| service-train-stock（StockService） | 1 | 8 | ✅ |
| service-ticket（TicketService） | 1 | 10 | ✅ |
| service-payment（PayService） | 1 | 6 | ✅ |
| service-order（OrderService） | 1 | 5 | ✅ |
| **合计** | **10** | **60** | **60/60 PASS** |

```bash
mvn -pl common,service-user,service-train-stock,service-ticket,service-payment,service-order test
→ 60/60 BUILD SUCCESS
```

---

## 1. 测试前修复的 Bug（Stage 11 联调前必看）

| # | 位置 | 修复 | 原因 |
|---|---|---|---|
| 1 | `common/WebAutoConfiguration` | 新增 `@Bean GlobalExceptionHandler` | common 的 `@ControllerAdvice` 不会被业务模块 `@SpringBootApplication` 默认扫到，全局异常处理失效 |
| 2 | `service-train-stock/TrainController.getSeats` | 加 `@AuthIgnore` | service-search 经 Feign 直连此端点，AuthInterceptor 默认拦截 |
| 3 | 6 个 service 的 `application.yml` | MySQL 密码 `root` → `123456` | 本机 MySQL 探测得到 root 密码为 123456 |

> 修 Bug 后所有模块 `mvn clean package` 仍 9/9 BUILD SUCCESS。

---

## 2. 单元测试（方案 C，已完成）

### 2.1 运行命令

```bash
# 全部
mvn -pl common,service-user,service-train-stock,service-ticket,service-payment,service-order test

# 单模块
mvn -pl common test

# 单测类
mvn -pl service-user test -Dtest=AuthServiceImplTest

# 单测方法
mvn -pl service-ticket test -Dtest=TicketServiceImplTest#issue_happyPath_createsTicketsAndPopsSeats
```

### 2.2 测试覆盖矩阵

| 模块 | 测试类 | 覆盖方法 | 关键场景 |
|---|---|---|---|
| common | `ErrorCodeTest` | 全部 enum | code 不重复、message 非空、段位归类正确、通用码 |
| common | `RTest` | `R.ok/fail` | 工厂方法、isSuccess、ErrorCode 转换 |
| common | `IdCardValidatorTest` | `isValid / mask` | 合法 / 非法 / 脱敏 / 短/null |
| common | `JwtUtilTest` | `generate/parse` | 签发解析往返、过期、非法、密钥 < 32 byte |
| common | `SnowflakeIdWorkerTest` | `nextId/setWorkerId` | 单调递增、不同 workerId、边界 |
| service-user | `AuthServiceImplTest` | `register/login` | 重复 / 新建 / 用户不存在 / 禁用 / 密码错 / 成功（带角色） |
| service-train-stock | `StockServiceImplTest` | `occupy/confirm/release` | happy / 库存不存在 / Lua 不足 / DB 乐观锁冲突 / release DB 冲突 warn / 释放无库存 |
| service-ticket | `TicketServiceImplTest` | `issue/confirm/cancel/getByTicketNo` | happy / 幂等 / 身份证不合法 / 座位池空兜底 / confirm 0/2 / cancel 退座位 / mask |
| service-payment | `PayServiceImplTest` | `createPay/callback/getByPayNo` | payNo + payUrl / 成功发 MQ / 幂等不发 / 失败发 MQ / 不存在抛错 |
| service-order | `OrderServiceImplTest` | `create` | happy / userId null / >5 人 / 库存不足 / 票失败补偿 |

### 2.3 单元测试关键设计

- **全部用 Mockito**（`@ExtendWith(MockitoExtension.class)` + `@Mock` + `@InjectMocks`），不连真实中间件。
- **Manager 层（OrderStockManager / OrderPayManager / OrderTicketManager / OrderUserManager）** 也 mock 掉，让 service 单测只测本类编排逻辑。
- **严格桩（strict stubbing）**：没用到的 `when(...)` 会被 Mockito 报错，避免错桩不查。
- **断言风格**：业务结果用 `assertEquals / assertNotNull / assertThrows`；调用次数用 `verify(...).times(...)`；用 `ArgumentCaptor` 抓取插入的实体做深度断言。

### 2.4 新增的 pom 依赖（每个 service 都要）

```xml
<!-- Test -->
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-test</artifactId>
    <scope>test</scope>
</dependency>
```
已加到 6 个 service 的 pom（service-user / service-train-stock / service-ticket / service-payment / service-order / service-notification / service-search）。

---

## 3. E2E 联调测试（方案 A，需中间件）

### 3.1 前置条件 — 中间件清单

| 中间件 | 版本要求 | 默认端口 | 用途 | 必须？ |
|---|---|---|---|---|
| **MySQL 8.x** | 8.0+ | 3306 | 主库 | ✅ |
| **Redis 7.x** | 7.0+ | 6379 | 登录态 / 库存 / 幂等 | ✅ |
| **Nacos 2.x** | 2.4+ | 8848 / 9848 | 服务发现 | ✅ |
| **RabbitMQ 3.x** | 3.12+ | 5672 / 15672 | 订单事件 | ✅ |
| **Elasticsearch** | 7.17+ | 9200 | 车次检索 | ❌（可降级） |
| **JDK** | 21 LTS | — | 运行环境 | ✅ |
| **Maven** | 3.9.x | — | 构建 | ✅ |

### 3.2 本机已装目录（检测到）

- `D:\mysql\mysql-8.0.27-winx64\` — MySQL ✅ 已在跑
- `D:\nacos\` — Nacos（**未启动**）
- `D:\RabbitMQ\` — RabbitMQ（**Get-Service 显示 Running**，但端口扫描 5672 失败）
- `D:\Redis\` — Redis（**未启动**）
- `D:\elasticsearch\` — ES（**未启动**）
- `D:\Redis_manager\` — Redis 桌面管理工具

### 3.3 中间件启动指引（Windows）

#### 3.3.1 MySQL（已就绪）
```powershell
# 检查
& "D:\mysql\mysql-8.0.27-winx64\bin\mysql.exe" -h 127.0.0.1 -uroot -e "SELECT VERSION();"
# 密码 123456

# 初始化 DB（如果没建）
$env:MYSQL_PWD = "123456"
cmd /c 'D:\mysql\mysql-8.0.27-winx64\bin\mysql.exe -h 127.0.0.1 -uroot --default-character-set=utf8mb4 --protocol=TCP < D:\idea2025\railway-real\db\init.sql'
```

#### 3.3.2 Nacos
```powershell
# 解压（如果只有 zip）
Expand-Archive -LiteralPath "D:\nacos\nacos-server-2.4.0.zip" -DestinationPath "D:\nacos\"

# 启动（standalone 模式）
cmd /c 'D:\nacos\nacos\bin\startup.cmd -m standalone'

# 验证（默认账号 nacos/nacos）
curl http://127.0.0.1:8848/nacos/
```

#### 3.3.3 Redis
```powershell
# 启动（前台或注册为服务）
cmd /c 'D:\Redis\redis-server.exe --port 6379'
# 或
cmd /c 'D:\Redis\redis-server.exe --service-install'
cmd /c 'D:\Redis\redis-server.exe --service-start'

# 验证
& "D:\Redis\redis-cli.exe" ping      # → PONG
```

#### 3.3.4 RabbitMQ（需 Erlang）
```powershell
# 启动管理插件（首次）
cmd /c 'D:\RabbitMQ\rabbitmq_server-3.x.x\sbin\rabbitmq-plugins.bat enable rabbitmq_management'

# 启动服务
Start-Service RabbitMQ      # 已是 Windows Service

# 验证
Test-NetConnection 127.0.0.1 -Port 5672
# 浏览器：http://127.0.0.1:15672  (guest/guest)
```

#### 3.3.5 Elasticsearch（可选，本演示不用）
```powershell
cmd /c 'D:\elasticsearch\bin\elasticsearch.bat'
# 验证
curl http://127.0.0.1:9200/
```

### 3.4 服务启动顺序

按依赖关系：先启基础设施，再启业务服务，**最后启 gateway**。

```powershell
# 0. 中间件（一次性，已在 3.3 启动）
# MySQL ✓ Redis ✓ Nacos ✓ RabbitMQ ✓ ES optional

# 1. 业务服务（独立进程，可后台）
$env:JAVA_HOME = "D:\java\jdk-21"
$env:MYSQL_PWD = "123456"

# 用 IDEA 启动 / 用 java -jar 启动 / 用 mvn spring-boot:run
# 推荐 java -jar（产物在 target/）

java -jar D:\idea2025\railway-real\service-user\target\service-user-1.0.0-SNAPSHOT.jar
java -jar D:\idea2025\railway-real\service-train-stock\target\service-train-stock-1.0.0-SNAPSHOT.jar
java -jar D:\idea2025\railway-real\service-ticket\target\service-ticket-1.0.0-SNAPSHOT.jar
java -jar D:\idea2025\railway-real\service-payment\target\service-payment-1.0.0-SNAPSHOT.jar
java -jar D:\idea2025\railway-real\service-order\target\service-order-1.0.0-SNAPSHOT.jar
java -jar D:\idea2025\railway-real\service-notification\target\service-notification-1.0.0-SNAPSHOT.jar
java -jar D:\idea2025\railway-real\service-search\target\service-search-1.0.0-SNAPSHOT.jar
java -jar D:\idea2025\railway-real\gateway\target\gateway-1.0.0-SNAPSHOT.jar
```

**预期启动日志**（每个服务）：
```
Started XxxApplication in X.XXX seconds (JVM running for XX.XXX)
Nacos registry, DEFAULT_GROUP service-xxx  xxx.xxx.xxx.xxx:910X register finished
```

**Nacos 控制台**（http://127.0.0.1:8848/nacos/）应能看到 7 个业务服务 + gateway = 8 个实例。

### 3.5 E2E 用例（按顺序执行）

> 所有请求经 **gateway:9000**，gateway 转发到各 service 业务端口。Token 放 `Authorization: Bearer <token>` 头。

#### T1. 注册（公开，无 token）

```bash
curl -i -X POST http://localhost:9000/api/user/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "username": "alice",
    "password": "P@ssw0rd",
    "phone": "13800000001",
    "email": "alice@example.com",
    "realName": "Alice"
  }'
```

**预期**：
```
HTTP/1.1 200
{"code":200,"message":"成功","data":1234567890123456,...}
```

**失败排查**：
- `{"code":1103,...}` → 用户名已存在
- 401 → 网关白名单失效（`gateway.public-paths` 没配 `/api/user/auth/register`）

#### T2. 重复注册 → 失败

```bash
curl -i -X POST http://localhost:9000/api/user/auth/register \
  -H "Content-Type: application/json" \
  -d '{"username":"alice","password":"any"}'
```

**预期**：`{"code":1103,"message":"用户已存在",...}`

#### T3. 登录拿 Token

```bash
curl -i -X POST http://localhost:9000/api/user/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"alice","password":"P@ssw0rd"}'
```

**预期**：
```json
{
  "code": 200,
  "message": "成功",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiJ9...",
    "userId": 1234567890123456,
    "username": "alice",
    "roles": ["USER"],
    "expireAt": 1730000000000
  }
}
```

**保存 token**：
```powershell
$env:TOKEN = (irm http://localhost:9000/api/user/auth/login -Method POST -Body (@{username='alice';password='P@ssw0rd'} | ConvertTo-Json -Compress) -ContentType 'application/json' -SkipHttpErrorCheck).data.token
```

#### T4. 错误密码登录 → 失败

```bash
curl -i -X POST http://localhost:9000/api/user/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"alice","password":"wrong"}'
```

**预期**：`{"code":1102,"message":"用户名或密码错误",...}`（不区分用户存在性，防枚举）

#### T5. /auth/me 无 Token → 401

```bash
curl -i http://localhost:9000/api/user/auth/me
```

**预期**：`{"code":401,"message":"未登录或登录已过期",...}`

#### T6. /auth/me 带 Token → 当前用户

```bash
curl http://localhost:9000/api/user/auth/me -H "Authorization: Bearer $TOKEN"
```

**预期**：
```json
{"code":200,"data":{"userId":...,"username":"alice","roles":["USER"]},...}
```

#### T7. 创建乘车人

```bash
curl -i -X POST http://localhost:9000/api/user/passengers \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "张三",
    "idCardType": 1,
    "idCardNo": "11010519491231002X",
    "phone": "13800000002",
    "passengerType": 0,
    "isDefault": 1
  }'
```

**预期**：`{"code":200,"data":1234567890123457,...}`（返回新建 passengerId）

#### T8. 创建第二个乘车人

```bash
curl -X POST http://localhost:9000/api/user/passengers \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "李四",
    "idCardType": 1,
    "idCardNo": "110101199003073570",
    "phone": "13800000003",
    "passengerType": 0
  }'
```

#### T9. 列乘车人（分页）

```bash
curl "http://localhost:9000/api/user/passengers?pageNum=1&pageSize=10" \
  -H "Authorization: Bearer $TOKEN"
```

**预期**：
```json
{
  "code": 200,
  "data": {
    "total": 2, "list": [
      {"id":..., "name":"张三", "idCardNo":"110105********002X", "isDefault":1,...},
      {"id":..., "name":"李四", "idCardNo":"110101********3570", "isDefault":0,...}
    ]
  }
}
```

> 身份证号已脱敏。

#### T10. 查车次（按 起始 / 终点 查，不需要 ES）

```bash
curl "http://localhost:9000/api/train/trains?startStation=北京南&endStation=上海虹桥" \
  -H "Authorization: Bearer $TOKEN"
```

**预期**：3 辆示例车（G1234 / G5678 / D2345，按车次类型过滤）。
> dev doc：service-train-stock 的 `TrainController.query` 用 `TrainQuery` 接收 query 参数。

#### T11. 查座位余票（**真实** Feign 链路：service-search → service-train-stock）

```bash
curl "http://localhost:9102/api/train/trains/G1234/seats?runDate=2026-06-10" \
  -H "Authorization: Bearer $TOKEN"
```

> 说明：本用例**绕过 gateway**，直连 service-train-stock（9102）。本机访问。

**预期**：
```json
{
  "code": 200,
  "data": {
    "BUSINESS": {"seatType":"BUSINESS","total":20,"remain":20},
    "FIRST":    {"seatType":"FIRST","total":50,"remain":50},
    "SECOND":   {"seatType":"SECOND","total":120,"remain":120}
  }
}
```

#### T12. ES 检索车次（**需要 ES**，未启时跳过）

```bash
# 灌一条数据到 ES
curl -X POST http://127.0.0.1:9200/train_index/_doc/G1234?refresh=true \
  -H "Content-Type: application/json" \
  -d '{
    "trainNo":"G1234","trainType":"G",
    "startStation":"北京南","startStationKeyword":"北京南",
    "endStation":"上海虹桥","endStationKeyword":"上海虹桥",
    "runDays":"1111111","runDate":"2026-06-10",
    "startTime":"2026-06-10T08:00:00","endTime":"2026-06-10T12:30:00",
    "prices":[{"seatType":"SECOND","price":1058.0}]
  }'

# 检索
curl "http://localhost:9000/api/search/search/trains?from=北京南&to=上海虹桥&date=2026-06-10" \
  -H "Authorization: Bearer $TOKEN"
```

**预期**：`[{"trainNo":"G1234",...}]`

#### T13. 下单（**核心** — Feign 编排 + 幂等）

```bash
curl -i -X POST http://localhost:9000/api/order/orders \
  -H "Authorization: Bearer $TOKEN" \
  -H "X-Idempotent-Key: idemp-$(Get-Date -Format 'yyyyMMddHHmmssfff')" \
  -H "Content-Type: application/json" \
  -d '{
    "trainNo": "G1234",
    "runDate": "2026-06-10",
    "seatType": "SECOND",
    "passengerIds": [1234567890123457, 1234567890123458],
    "price": 1058.00
  }'
```

**预期**：
```json
{
  "code": 200,
  "data": {
    "orderNo": "O1234567890123456",
    "amount": 2116.00,
    "expireTime": "2026-06-10T15:30:00",
    "payUrl": "http://localhost:9000/api/payment/callback/sim?payNo=Pxxx"
  }
}
```

**保存 orderNo**：
```powershell
$env:ORDER_NO = "Oxxx"
```

**关键验证**（同步查 DB）：
```sql
-- 订单应 status=0
SELECT order_no, status, amount, expire_time FROM orders WHERE order_no = 'Oxxx';
-- 库存应 -2（SECOND 类型 120 → 118）
SELECT train_no, run_date, seat_type, remain, version FROM train_seat_stock
WHERE train_no='G1234' AND run_date='2026-06-10' AND seat_type='SECOND';
-- 票应 2 张，status=0
SELECT order_no, ticket_no, status FROM ticket WHERE order_no='Oxxx';
-- 流水应有 1 条 occupy (biz_type=1, delta=-2)
SELECT order_no, biz_type, delta FROM stock_flow WHERE order_no='Oxxx';
```

#### T14. 重复下单（**同 X-Idempotent-Key**）→ 幂等命中

```bash
curl -i -X POST http://localhost:9000/api/order/orders \
  -H "Authorization: Bearer $TOKEN" \
  -H "X-Idempotent-Key: idemp-fixed-key" \
  -H "Content-Type: application/json" \
  -d '{...同上...}'
# 立刻再发一次
curl -i -X POST http://localhost:9000/api/order/orders \
  -H "Authorization: Bearer $TOKEN" \
  -H "X-Idempotent-Key: idemp-fixed-key" \
  -H "Content-Type: application/json" \
  -d '{...同上...}'
```

**预期**：第 1 次 200，第 2 次 `{"code":1001,"message":"重复请求",...}`（IDEMPOTENT_REPEAT）

#### T15. 库存不足 → 失败

```bash
# 把 SECOND 库存预占 119 张（通过 SQL 模拟 1 张）
UPDATE train_seat_stock SET remain = 1 WHERE train_no='G1234' AND run_date='2026-06-10' AND seat_type='SECOND';

# 下 2 张 → 应失败
curl -i -X POST http://localhost:9000/api/order/orders \
  -H "Authorization: Bearer $TOKEN" \
  -H "X-Idempotent-Key: idemp-fail-$(Get-Date -Format 'yyyyMMddHHmmssfff')" \
  -H "Content-Type: application/json" \
  -d '{"trainNo":"G1234","runDate":"2026-06-10","seatType":"SECOND","passengerIds":[...], "price":1058.00}'

# 还原
UPDATE train_seat_stock SET remain = 120 WHERE train_no='G1234' AND run_date='2026-06-10' AND seat_type='SECOND';
```

**预期**：`{"code":2001,"message":"库存不足",...}`（STOCK_NOT_ENOUGH）
> 库存检查在 Feign `stock.occupy` 的 Redis Lua 阶段，不应改 DB 库存。

#### T16. 模拟支付成功

打开 payUrl（浏览器或 curl），触发回调：

```bash
curl -X POST "http://localhost:9000/api/payment/callback/sim?payNo=Pxxx" \
  -H "Content-Type: application/json" \
  -d '{"payNo":"Pxxx","success":true}'
```

> 实际上 payUrl 的 `payNo=...` query 参数会被 controller 解析，可能不需要 body。看 `service-payment/PayController.callback` 实现。

**预期**：
- pay_record status 0 → 1
- 发 `order.paid` 到 RabbitMQ
- service-order 收到 → order status 0 → 1
- 调 stock.confirm（写流水，DB 不变）
- 调 ticket.confirm → ticket status 0 → 1
- service-notification 收到 → 落 `notification_log`

**验证 SQL**：
```sql
SELECT status, paid_time FROM pay_record WHERE order_no='Oxxx';     -- 1
SELECT status FROM orders WHERE order_no='Oxxx';                    -- 1
SELECT status FROM ticket WHERE order_no='Oxxx';                    -- 1
SELECT type, status, content FROM notification_log WHERE order_no='Oxxx';  -- 0,0,'【铁路购票】您订单 Oxxx 支付成功，金额 ¥2116.00...'
```

#### T17. 重复支付回调 → 幂等命中

```bash
# 立即再发一次
curl -X POST "http://localhost:9000/api/payment/callback?payNo=Pxxx" \
  -H "Content-Type: application/json" \
  -d '{"payNo":"Pxxx","success":true}'
```

**预期**：pay_record 仍 1 条 status=1；**不会**重复发 `order.paid`（callback SQL 幂等：affected=0 → return false → 不发 MQ）。
> notification_log **可能**多 1 条（因为 service-notification 的 `@RabbitListener` 也对同一 MQ 消息处理了 1 次，但生产上 consumer 端应该用业务幂等键去重；demo 阶段不去重）。

#### T18. 查订单详情

```bash
curl "http://localhost:9000/api/order/orders/$env:ORDER_NO" -H "Authorization: Bearer $TOKEN"
```

**预期**：
```json
{
  "code": 200,
  "data": {
    "order": { "orderNo": "Oxxx", "status": 1, "amount": 2116.00, ... },
    "tickets": []   // demo 阶段留空（OrderDetailVO.tickets 暂未调 ticket Feign）
  }
}
```

#### T19. 取消订单（**status=0 待支付**才可取消）

> 当前订单 status=1（已支付），不能再走 `/cancel` 测试。需要新建一个订单保持 status=0。

```bash
# 新建订单（同 T13 步骤）
$env:ORDER_NO2 = "Oyyy"

# 立即取消（不支付）
curl -X POST "http://localhost:9000/api/order/orders/$env:ORDER_NO2/cancel" \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"reason":"test cancel"}'
```

**预期**：
- order status 0 → 2
- stock.release（写流水 +1）
- ticket.cancel（status 0/1 → 3，座位退到 Redis Set）
- 库存 SECOND 应 +2

**验证 SQL**：
```sql
SELECT status FROM orders WHERE order_no='Oyyy';     -- 2
SELECT remain FROM train_seat_stock WHERE train_no='G1234' AND run_date='2026-06-10' AND seat_type='SECOND';  -- 比 T13 之前 +2
SELECT status FROM ticket WHERE order_no='Oyyy';     -- 3
```

#### T20. 别人的订单 → 403

```bash
# 用 bob 登录，再查 alice 的订单
$env:TOKEN_BOB = ...
curl "http://localhost:9000/api/order/orders/$env:ORDER_NO" -H "Authorization: Bearer $env:TOKEN_BOB"
```

**预期**：`{"code":403,"message":"无权查看该订单",...}`

#### T21. 超时关单（**15min 等待太长**，可手动改短）

> dev 文档 §11 提到 15min 自动关单（MQ TTL）。压测时可改 `order.pay-expire-minutes: 1`（1 分钟），重启 service-order。

等待 1 分钟后查订单：
```sql
SELECT status FROM orders WHERE order_no='Oxxx';   -- status=3 (退款/关闭)
SELECT remain FROM train_seat_stock WHERE train_no='G1234' AND run_date='2026-06-10' AND seat_type='SECOND';  -- 库存释放
SELECT status FROM ticket WHERE order_no='Oxxx';   -- 3
```

#### T22. 限流压测（可选）

```bash
# 100 并发抢 G1234 SECOND (120 座) → 期望 0 超卖
# 用 JMeter 或 ab
ab -n 100 -c 50 -p order.json -T application/json -H "Authorization: Bearer $TOKEN" -H "X-Idempotent-Key: ab-$(uuidgen)" http://localhost:9000/api/order/orders
```

**预期**：
- 总成功数 = 60（120 / 2 = 60 单，每单 2 张票）
- 总失败数 = 40（`STOCK_NOT_ENOUGH`）
- 库存最终 = 0
- 订单数 = 60
- 票数 = 120

---

## 4. 故障排查 Checklist

| 症状 | 排查点 |
|---|---|
| gateway 返 401 | Token 失效 → 重新登录；Token 格式 → `Bearer <token>`；gateway 白名单（`gateway.public-paths`） |
| gateway 返 404 | 路径前缀错（gateway StripPrefix=2）→ 实际调 `/xxx`；service 未启动 |
| service 启动失败：Nacos 拒连 | `127.0.0.1:8848` 通否；nacos 启动完成（启动慢，需等 30s+） |
| service 启动失败：Redis 拒连 | `127.0.0.1:6379` 通否；密码（demo 无密码） |
| service 启动失败：MySQL 拒连 | 密码错（应是 123456）；`useSSL=false`；时区 `Asia/Shanghai` |
| service 启动失败：Rabbit 拒连 | `127.0.0.1:5672` 通否；`guest/guest`；vhost `/` |
| 下单返 2001 库存不足 | Redis Lua 失败 → 检查 stock 预占逻辑；DB `remain` 确实为 0 |
| 下单返 5001 出票失败 | 身份证号不合法 / Redis 座位池空 |
| 支付返 4002 支付单不存在 | payNo 错；DB 真的没插 |
| 通知没收到 | service-notification 没启；MQ retry 3 次后丢弃；查 `notification_log` 表 |

---

## 5. 总结

- **方案 C（单元测试）已完成 60/60 PASS**。
- **方案 A（E2E 文档）已写完**：本文档 §3 是完整步骤。
- **要跑 E2E**：先按 §3.3 启 4 个中间件 → §3.4 启服务 → §3.5 按 T1~T22 执行。
- **预期完成时间**：启中间件 10min + 启服务 5min + 跑用例 30min = **约 45min**。
