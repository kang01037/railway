# 铁路购票系统 - API 接口文档

> **最后更新**：2026-06-06
> **项目版本**：`1.0.0-SNAPSHOT`
> **统一返回**：`R<T>` = `{ code, message, data, success, traceId }`（common）
> **统一网关**：`http://localhost:9000`（gateway:9000，StripPrefix=2 → `/{service-prefix}/**`）

---

## 0. 路由总览（gateway）

gateway yml 的 7 条路由（**StripPrefix=2**）：

| gateway path prefix | service-name | service 端口 | controller 根路径 |
|---|---|---|---|
| `/api/user/**` | service-user | 9101 | `/auth/**`、`/passengers/**` |
| `/api/train/**` | service-train-stock | 9102 | `/trains/**`、`/stock/**` |
| `/api/ticket/**` | service-ticket | 9103 | `/tickets/**` |
| `/api/payment/**` | service-payment | 9104 | `/pay/**` |
| `/api/order/**` | service-order | 9105 | `/orders/**` |
| `/api/notification/**` | service-notification | 9106 | `/notifications/**`（如启用） |
| `/api/search/**` | service-search | 9210 | `/search/**` |

> 示例：`POST /api/user/auth/login`（gateway） → `POST /auth/login`（service-user:9101）

---

## 1. 公共约定

### 1.1 鉴权头

| 头 | 必填 | 说明 |
|---|---|---|
| `Authorization: Bearer <token>` | 是（除 `@AuthIgnore` 外） | JWT token，登录后获取 |
| `X-Idempotent-Key` | 仅下单接口 | 幂等键，60s TTL，重复 → `1001 IDEMPOTENT_REPEAT` |

gateway 透传 `X-User-Id` / `X-User-Name` / `X-User-Roles`（在 token 解析时注入），业务服务 `AuthInterceptor` 优先读头，回退到 JWT 解析。

### 1.2 统一返回 `R<T>`

```json
{
  "code": 200,
  "message": "成功",
  "data": <T>,
  "success": true,
  "traceId": "abc12345"
}
```

### 1.3 错误码

| 范围 | 域 | 关键码 |
|---|---|---|
| 200/4xx/5xx | 通用 | 200 成功 / 400 参数错 / 401 未登录 / 403 无权限 / 404 不存在 / 500 服务器错 |
| 1000-1099 | 通用业务 | 1000 业务异常 / 1001 重复请求（幂等） |
| 1100-1199 | 用户域 | 1101 用户不存在 / 1102 密码错 / 1103 用户已存在 / 1104 禁用 / 1105 未登录 |
| 1200-1299 | 乘车人 | 1201 乘车人不存在 |
| 1300-1399 | 车次 | 1301 车次不存在 / 1302 座位类型无效 |
| 2000-2099 | 库存 | 2001 库存不足 / 2002 预占失败 / 2003 座位库存不存在 / 2004 版本冲突 |
| 3000-3099 | 订单 | 3001 订单不存在 / 3002 已过期 / 3003 已支付 / 3004 取消失败 / 3005 创建失败 |
| 4000-4099 | 支付 | 4001 支付失败 / 4002 支付单不存在 / 4003 金额错 |
| 5000-5099 | 票 | 5001 出票失败 / 5002 票不存在 |

---

## 2. service-user（9101）

### 2.1 认证 `/api/user/auth/**`

#### `POST /api/user/auth/register` — 注册（`@AuthIgnore`）

**请求**：
```json
{
  "username": "alice",
  "password": "P@ssw0rd",
  "phone": "13800000001",
  "email": "alice@example.com",
  "realName": "Alice"
}
```
**响应**：`R<Long>`（userId）
**错误**：`1103 USER_ALREADY_EXISTS` 用户名已存在

#### `POST /api/user/auth/login` — 登录（`@AuthIgnore`）

**请求**：
```json
{ "username": "alice", "password": "P@ssw0rd" }
```
**响应**：
```json
{
  "code": 200,
  "data": {
    "token": "eyJhbGciOiJIUzI1NiJ9...",
    "userId": 1234567890123456,
    "username": "alice",
    "roles": ["USER"],
    "expireAt": 1730000000000
  }
}
```
**错误**：`1102 USER_PASSWORD_ERROR`（不区分用户存在性，防枚举）

#### `POST /api/user/auth/logout` — 登出

**响应**：`R<Void>`

#### `GET /api/user/auth/me` — 当前用户

**响应**：`R<LoginUser>`（来自 common，含 userId / username / roles）

---

### 2.2 乘车人 `/api/user/passengers/**`

#### `POST /api/user/passengers` — 新增

**请求** `PassengerReq`：
```json
{
  "name": "张三",
  "idCardType": 1,
  "idCardNo": "11010519491231002X",
  "phone": "13800000002",
  "passengerType": 0,    // 0 成人 / 1 儿童 / 2 学生
  "isDefault": 1         // 0 否 / 1 是
}
```
**响应**：`R<Long>`（passengerId）

#### `GET /api/user/passengers?pageNum=1&pageSize=10` — 分页查询

**Query**：`pageNum` / `pageSize`（继承 `PageQuery`），可选 `passengerType`
**响应**：`R<PageInfo<PassengerVO>>`，身份证号已脱敏（`110105********002X`）

#### `GET /api/user/passengers/{id}` — 查单个

#### `PUT /api/user/passengers/{id}` — 更新（body 同 `PassengerReq`）

#### `DELETE /api/user/passengers/{id}` — 删除

---

## 3. service-train-stock（9102）

### 3.1 车次管理（管理员）`/api/train/trains/**`

> 仅 `@RequireRole("ADMIN")` 的端点

| Method | Path | 说明 |
|---|---|---|
| `POST` | `/api/train/trains` | 新增车次（含初始库存） |
| `PUT` | `/api/train/trains/{id}` | 修改车次 |
| `DELETE` | `/api/train/trains/{id}` | 删除车次 |
| `GET` | `/api/train/trains/{id}` | 按 ID 查（任意用户） |
| `GET` | `/api/train/trains?startStation=...&endStation=...&status=0/1` | 条件查询（任意用户） |

**`TrainReq` 示例**：
```json
{
  "trainNo": "G1234",
  "trainType": "G",
  "startStation": "北京南",
  "endStation": "上海虹桥",
  "startTime": "2026-06-10T08:00:00",
  "endTime": "2026-06-10T12:30:00",
  "runDays": "1111111",
  "status": 1,
  "initialStocks": [
    { "runDate": "2026-06-10", "seatType": "SECOND", "total": 120 }
  ]
}
```

### 3.2 座位余票 `/api/train/trains/{trainNo}/seats`（`@AuthIgnore`）

**GET** `/api/train/trains/G1234/seats?runDate=2026-06-10`

**响应**：
```json
{
  "code": 200,
  "data": [
    { "id": 1, "trainNo": "G1234", "runDate": "2026-06-10", "seatType": "BUSINESS", "total": 20, "remain": 20 },
    { "id": 2, "trainNo": "G1234", "runDate": "2026-06-10", "seatType": "FIRST",    "total": 50, "remain": 50 },
    { "id": 3, "trainNo": "G1234", "runDate": "2026-06-10", "seatType": "SECOND",   "total": 120, "remain": 120 }
  ]
}
```

> **`@AuthIgnore` 原因**：service-search 经 Feign 直连此端点（不能经 gateway 注入 X-User-*）。

### 3.3 库存（仅 Feign 调用）`/api/train/stock/**`（`@AuthIgnore`）

| Method | Path | Body | 返回 |
|---|---|---|---|
| `POST` | `/api/train/stock/occupy` | `OccupyDTO{orderNo,trainNo,runDate,seatType,num}` | `R<OccupyVO>{stockKey,remainAfter}` |
| `POST` | `/api/train/stock/confirm` | `ConfirmDTO{orderNo,trainNo,runDate,seatType,num}` | `R<Void>` |
| `POST` | `/api/train/stock/release` | `ReleaseDTO{orderNo,trainNo,runDate,seatType,num}` | `R<Void>` |

**错误**：`2001 STOCK_NOT_ENOUGH` / `2002 STOCK_OCCUPY_FAILED` / `2003 STOCK_NOT_FOUND` / `2004 STOCK_VERSION_CONFLICT`

---

## 4. service-ticket（9103）

### 4.1 出票（仅 Feign）`/api/ticket/tickets/**`（`@AuthIgnore`）

| Method | Path | Body | 返回 |
|---|---|---|---|
| `POST` | `/api/ticket/tickets/issue` | `IssueDTO{orderNo,trainNo,runDate,seatType,price,passengers:[{passengerId,passengerName,idCardNo}]}` | `R<IssueVO>{orderNo,ticketCount,ticketNos[]}` |
| `POST` | `/api/ticket/tickets/confirm` | `ConfirmDTO{orderNo}` | `R<Integer>`（affected rows） |
| `POST` | `/api/ticket/tickets/cancel` | `CancelDTO{orderNo}` | `R<Integer>`（affected rows） |

**错误**：`5001 TICKET_ISSUE_FAILED` / `5002 TICKET_NOT_FOUND`

### 4.2 用户查询

#### `GET /api/ticket/tickets/{ticketNo}` — 按票号查

#### `GET /api/ticket/tickets/order/{orderNo}` — 按订单号查（多张）

**`TicketVO` 字段**：
```json
{
  "id": 1,
  "ticketNo": "T17000000000001234",
  "orderNo": "O17000000000001234",
  "trainNo": "G1234",
  "runDate": "2026-06-10",
  "seatType": "SECOND",
  "carriageNo": "03",
  "seatNo": "12A",
  "passengerId": 1234567890123457,
  "passengerName": "张三",
  "idCardNo": "110105********002X",
  "price": 1058.0,
  "status": 1,        // 0 待支付 / 1 已出票 / 2 已改签 / 3 已退
  "createTime": "2026-06-10T08:01:23"
}
```

---

## 5. service-payment（9104）

### 5.1 支付 `/api/payment/pay/**`

#### `POST /api/payment/pay/create` — 创建支付单

**请求** `CreatePayDTO`：
```json
{ "orderNo": "O17000000000001234", "amount": 2116.00, "payChannel": "SIM" }
```

**响应** `PayVO`：
```json
{
  "payNo": "P17000000000005678",
  "orderNo": "O17000000000001234",
  "amount": 2116.00,
  "payChannel": "SIM",
  "payUrl": "http://localhost:9000/api/payment/callback/sim?payNo=P17000000000005678",
  "status": 0,         // 0 待支付 / 1 成功 / 2 失败
  "paidTime": null,
  "createTime": "2026-06-10T08:01:24"
}
```

#### `POST /api/payment/pay/callback/sim` — 模拟回调（`@AuthIgnore`）

**请求** `CallbackDTO`：
```json
{ "payNo": "P17000000000005678", "success": true }
```

**行为**：
- status `0 → 1`，写 `paid_time`
- 发 MQ `order.paid` 到 `orderExchange`（RK `order.paid`）
- **幂等**：状态非 0 不再发 MQ

**响应**：
```json
{ "code": 200, "data": { "payNo": "P17000000000005678", "processed": true } }
```

> `gateway.public-paths` 已配白名单 `/api/payment/callback/**`，可被第三方回调。

#### `GET /api/payment/pay/{payNo}` — 按 payNo 查

#### `GET /api/payment/pay/order/{orderNo}` — 按 orderNo 查（可能多条）

**错误**：`4001 PAY_FAILED` / `4002 PAY_NOT_FOUND` / `4003 PAY_AMOUNT_ERROR`

---

## 6. service-order（9105）

### 6.1 下单 `/api/order/orders`

#### `POST /api/order/orders` — 创建订单 ⭐核心

**Headers**：
- `Authorization: Bearer <token>` 必填
- `X-Idempotent-Key: <uuid>` **必填**（60s TTL，重复 → `1001`）

**请求** `CreateOrderDTO`：
```json
{
  "trainNo": "G1234",
  "runDate": "2026-06-10",
  "seatType": "SECOND",
  "passengerIds": [1234567890123457, 1234567890123458],
  "price": 1058.00
}
```

**编排**（OrderServiceImpl.create）：
1. Redis Lua 幂等（`IDEMPOTENT:{key}` SETNX）
2. 写 `orders`（status=0 待支付） + `order_passenger`
3. Feign 调 `stock.occupy`（Lua + DB 乐观锁）
4. Feign 调 `ticket.issue`
5. **补偿**：如果 4 失败 → `stock.release` + `orders` 标记失败 + `order_passenger` 删
6. 发 MQ `order.delay`（TTL 15min）→ 到期 → `order.cancel` 关单

**响应** `CreateOrderResultVO`：
```json
{
  "orderNo": "O17000000000001234",
  "payUrl": "http://localhost:9000/api/payment/callback/sim?payNo=P17000000000005678",
  "amount": 2116.00,
  "expireTime": "2026-06-10T15:30:00"
}
```

**错误**：
- `1001 IDEMPOTENT_REPEAT` 重复请求（同 X-Idempotent-Key）
- `2001 STOCK_NOT_ENOUGH` 库存不足
- `5001 TICKET_ISSUE_FAILED` 出票失败（已补偿）
- `3005 ORDER_CREATE_FAILED` 编排失败

### 6.2 订单详情

#### `GET /api/order/orders/{orderNo}` — 按订单号查

**响应** `OrderDetailVO`：
```json
{
  "code": 200,
  "data": {
    "order": {
      "id": 1, "orderNo": "O17000000000001234", "userId": 1234567890123456,
      "trainNo": "G1234", "runDate": "2026-06-10", "seatType": "SECOND",
      "num": 2, "amount": 2116.00,
      "status": 1,         // 0 待支付 / 1 已支付 / 2 已取消 / 3 已关闭
      "expireTime": "2026-06-10T15:30:00",
      "createTime": "...", "updateTime": "..."
    },
    "tickets": []
  }
}
```

> `tickets` 字段在 demo 阶段留空（OrderDetailVO.tickets 未调 ticket Feign）。

#### `GET /api/order/orders?pageNum=1&pageSize=10` — 当前用户订单分页

### 6.3 取消订单

#### `POST /api/order/orders/{orderNo}/cancel`

**请求** `CancelOrderDTO`：
```json
{ "orderNo": "O17000000000001234", "reason": "user cancel" }
```

**行为**：
- order status `0 → 2`（已取消）
- 调 `stock.release`（库存 +2，写流水）
- 调 `ticket.cancel`（票 status `0/1 → 3`）
- **限制**：status `0` 才能取消，其他 → `3004 ORDER_CANCEL_FAILED`

---

## 7. service-search（9210）— ES 检索

> port 改 9210（避 ES 9200）。`@AuthIgnore`（gateway 层已鉴权）。

### 7.1 `GET /api/search/search/trains` — 车次模糊查

**Query**：
- `from` (String, optional) — 起始站
- `to` (String, optional) — 终点站
- `date` (yyyy-MM-dd, optional) — 适用日期

**响应** `TrainIndex` ES 文档：
```json
{
  "code": 200,
  "data": [
    {
      "id": "G1234",
      "trainNo": "G1234", "trainType": "G",
      "startStation": "北京南", "startStationKeyword": "北京南",
      "endStation": "上海虹桥", "endStationKeyword": "上海虹桥",
      "startTime": "2026-06-10T08:00:00",
      "endTime":   "2026-06-10T12:30:00",
      "runDays": "1111111", "runDate": "2026-06-10",
      "prices": [{ "seatType": "SECOND", "price": 1058.0 }]
    }
  ]
}
```

> **ES 灌数据**（需先 `PUT train_index` mapping 后）：
> ```bash
> curl -X POST "http://127.0.0.1:9200/train_index/_doc/G1234?refresh=true" \
>   -H "Content-Type: application/json" --data-binary @es-seed.json
> ```
> 详见 `测试文档.md` §3.5 T12。

### 7.2 `GET /api/search/search/trains/{trainNo}/seats?date=2026-06-10` — 座位余票

**响应** `Map<seatType, SeatRemainVO>`：
```json
{ "code": 200, "data": { "SECOND": { "seatType": "SECOND", "total": 120, "remain": 118 } } }
```

> 此接口走 Feign → service-train-stock (`/api/train/trains/{no}/seats`)

---

## 8. service-notification（9106）

> 本服务**无 controller**，仅 MQ 消费者，详见 `测试文档.md` T16 验证 `notification_log` 表。

---

## 9. 调用时序图

### 9.1 完整下单流程

```
[Client] → POST /api/order/orders (X-Idempotent-Key=abc)
   ↓ gateway:9000
[service-order] IdempotentAspect → Redis SETNX abc (60s)
   ↓
[service-order] insert orders(status=0) + order_passenger
   ↓ Feign
[service-train-stock] /stock/occupy
   ↓ Redis Lua + DB optimistic lock
[service-ticket] /tickets/issue (return ticketNos[])
   ↓ (issue fail → /stock/release 补偿)
[service-order] send MQ order.delay (TTL=15min) → orderExchange → orderCancelQueue (DLX)
   ↓ return CreateOrderResultVO

--- 客户端拿到 payUrl，打开回调 ---

[Third party] → POST /api/payment/callback/sim?payNo=... (gateway 白名单)
   ↓
[service-payment] update pay_record status=1 + send MQ order.paid
   ↓ MQ 消费
[service-order] OrderPaidConsumer
   ↓
   ├─ /stock/confirm
   ├─ /tickets/confirm
   └─ 写 order.status=1
   ↓ MQ 消费
[service-notification] OrderPaidConsumer → notification_log(type=0, status=0)

--- 15 分钟未支付 ---

MQ TTL 到期 → orderExchange → orderCancelQueue
   ↓
[service-order] OrderDelayCancelConsumer
   ├─ order.status=3 (关闭)
   ├─ /stock/release
   └─ /tickets/cancel (status → 3)
```

---

## 10. 速查表（curl 模板）

### 10.1 完整购票 5 步

```bash
# 1. 注册
curl -X POST http://localhost:9000/api/user/auth/register \
  -H "Content-Type: application/json" \
  -d '{"username":"alice","password":"P@ssw0rd","phone":"13800000001","email":"a@b.c","realName":"Alice"}'

# 2. 登录拿 token
TOKEN=$(curl -sX POST http://localhost:9000/api/user/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"alice","password":"P@ssw0rd"}' | jq -r .data.token)

# 3. 创建乘车人
PASSENGER_ID=$(curl -sX POST http://localhost:9000/api/user/passengers \
  -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" \
  -d '{"name":"张三","idCardType":1,"idCardNo":"11010519491231002X","phone":"13800000002","passengerType":0,"isDefault":1}' \
  | jq -r .data)

# 4. 下单（核心）
ORDER=$(curl -sX POST http://localhost:9000/api/order/orders \
  -H "Authorization: Bearer $TOKEN" \
  -H "X-Idempotent-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d "{\"trainNo\":\"G1234\",\"runDate\":\"2026-06-10\",\"seatType\":\"SECOND\",\"passengerIds\":[$PASSENGER_ID],\"price\":1058.00}")
ORDER_NO=$(echo $ORDER | jq -r .data.orderNo)
PAY_URL=$(echo $ORDER | jq -r .data.payUrl)

# 5. 模拟支付回调
PAY_NO=$(echo $PAY_URL | grep -oE 'payNo=[^&]+' | cut -d= -f2)
curl -X POST "http://localhost:9000/api/payment/callback/sim?payNo=$PAY_NO" \
  -H "Content-Type: application/json" \
  -d "{\"payNo\":\"$PAY_NO\",\"success\":true}"
```

### 10.2 取消订单

```bash
curl -X POST http://localhost:9000/api/order/orders/$ORDER_NO/cancel \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d "{\"orderNo\":\"$ORDER_NO\",\"reason\":\"user cancel\"}"
```

### 10.3 ES 查车次

```bash
curl "http://localhost:9000/api/search/search/trains?from=北京南&to=上海虹桥&date=2026-06-10"
```

---

## 11. 备注

| 主题 | 说明 |
|---|---|
| **路径前缀** | gateway StripPrefix=2 → 业务 service 真实路径比网关少 2 段（`/api/{service}`） |
| **token 注入** | gateway 在请求转发时由 AuthGlobalFilter 解析 JWT → 注入 `X-User-Id` / `X-User-Name` / `X-User-Roles` |
| **Feign 内部调用** | 全部 `@AuthIgnore`（`stock` / `ticket.issue` / `payment.callback` / `search`），不需 token |
| **幂等** | 仅 `POST /orders` 强制 `X-Idempotent-Key` |
| **白名单** | gateway yml `gateway.public-paths`：`/api/user/auth/login`、`/api/user/auth/register`、`/api/payment/callback/**` |
| **直连 vs 网关** | 测试可绕过 gateway 直接打 service 端口（注意：service 内 `AuthInterceptor` 仍要 token 或 `@AuthIgnore`） |
