# 铁路购票系统 —— 开发文档

> 本文档依据 `项目结构.md` 编写，逐阶段说明**先做哪个模块、每个步骤要完成什么、关键代码/配置长什么样、如何验证**。
> 技术栈固定为：**Spring Cloud + MyBatis + Nacos + OpenFeign + RabbitMQ + Redis + Elasticsearch + JWT + 雪花算法 + BCrypt**，其中 **Redis + Lua 脚本** 用于库存的原子预占/释放。
>
> ORM 用**原生 MyBatis**（Mapper 接口 + XML），**不用 MyBatis-Plus**；分页用 **PageHelper**。

---

## 0. 阅读须知

- 文档按 **开发顺序** 编排章节，**必须按顺序阅读并按顺序开发**，后置阶段会引用前置阶段产出的 Bean / API。
- 每个阶段都包含：**目标 / 文件清单 / 关键实现 / 自测方式**。
- 文中所有 `xxx` 代表具体名称（车次号、用户名等），`{bizCode}` 自行替换。
- 端口规划：网关 `9000`、user `9101`、train-stock `9102`、ticket `9103`、payment `9104`、order `9105`、notification `9106`、MySQL `3306`、Redis `6379`、Nacos `8848/9848`、RabbitMQ `5672/15672`、ES `9200`。
- 数据库名：`railway-real`（与项目 artifactId 一致），统一字符集 `utf8mb4`。

---

## 1. 整体架构

```
                        ┌──────────────┐
                        │  PC / 小程序 │
                        └──────┬───────┘
                               │ HTTPS
                        ┌──────▼───────┐
                        │  gateway     │ 9000  鉴权 / 路由 / 限流 / TraceId
                        └──┬───┬───┬───┘
                           │   │   │
        ┌──────────────────┘   │   └────────────────────┐
        ▼                      ▼                        ▼
 service-user           service-train-stock        service-ticket
   9101                    9102 (Redis+Lua)         9103
        │                      │                        │
        └────────┬─────────────┴────────┬───────────────┘
                 ▼                      ▼
         service-payment          service-order (Feign 编排)
            9104                      9105
                                       │
                                       ▼
                            RabbitMQ (订单事件)
                                       │
                                       ▼
                             service-notification
                                    9106

  中间件：Nacos(配置/注册)、Redis(缓存/分布式锁/Lua)、RabbitMQ(异步解耦/延迟消息)、
         Elasticsearch(车次/票检索)、MySQL(持久化)
```

依赖方向：**common  ←  所有业务服务  ←  gateway**。

---

## 2. 技术栈版本矩阵

| 组件 | 版本 | 用途 |
|---|---|---|
| Spring Boot | 3.5.14 | 基础 |
| Spring Cloud | 2025.0.x | 微服务 |
| Spring Cloud Alibaba | 2025.0.0.x | Nacos / OpenFeign |
| MyBatis | 3.0.3（boot3 配套） | ORM |
| MySQL | 8.x | 主库 |
| PageHelper | 2.1.0 | 物理分页 |
| Nacos | 2.x | 注册/配置中心 |
| OpenFeign | 4.x | 服务间调用 |
| Sentinel | 1.8.x | 限流熔断（按需） |
| RabbitMQ | 3.13.x | 异步消息 + 延迟队列 |
| Redis | 7.x | 缓存 + 分布式锁 + Lua |
| Elasticsearch | 8.x | 车次/票检索 |
| jjwt | 0.12.x | JWT 签发/校验 |
| BCrypt | Spring Security Crypto | 密码加密 |
| Lombok | 1.18.x | 简化代码 |
| Maven | 3.9+ | 构建 |
| JDK | 21 | 与父 POM `java.version` 一致 |

---

## 3. 模块开发顺序总览

> **核心原则**：**先地基、后业务；先被依赖、后调用方；先有数据、后有编排。**

| 阶段 | 模块 | 状态 | 关键产出 | 被谁依赖 |
|---|---|---|---|---|
| ① | **父工程 POM** | 无代码 | 依赖版本、聚合 7 个子模块 | 所有 |
| ② | **common** | 工具类 | R、异常、JWT、雪花、BCrypt、拦截器、AOP、Redis/MyBatis 配置 | 所有服务 |
| ③ | **service-user** | 最简业务 | 注册/登录/乘车人 | order、ticket |
| ④ | **gateway** | 入口 | 路由、JWT 透传、跨域 | 前端 |
| ⑤ | **service-train-stock** | 核心基础数据 + Lua 原子 | 车次 CRUD、库存预占/确认/释放 | order |
| ⑥ | **service-ticket** | 票实体 | 生成/确认/取消票 | order |
| ⑦ | **service-payment** | 独立支付 | 发起支付/回调 | order |
| ⑧ | **service-order** | **核心编排** | 下单/取消/查询（Feign + 消息） | notification |
| ⑨ | **service-notification** | 异步消费 | 邮件/短信（RabbitMQ Consumer） | — |
| ⑩ | **Elasticsearch 增强** | 检索 | 车次/票搜索 | — |
| ⑪ | **联调 / 压测** | 整体 | 端到端用例 | — |

> 简单说：**先打通一条"能注册登录 + 看到车次"的主干，再做下单 → 支付 → 出票 → 通知**。

---

## 4. 阶段一：父工程搭建

### 4.1 目标
- 把当前单模块工程改造成 **7 模块 Maven 聚合工程**。
- 用 `dependencyManagement` 统一所有组件版本，子模块 **不再写版本号**。

### 4.2 操作步骤

**步骤 1.1**：删除原 `src/` 目录（保留 `.mvn`、`mvnw`、`pom.xml`、`.gitignore`）。

**步骤 1.2**：在根 `pom.xml` 中将 `groupId` 改为 `com.railway`，`artifactId` 改为 `railway-ticket`，`packaging` 改为 `pom`，并加入：
- `<modules>` 聚合 7 个子模块
- `<properties>` 中固定 `spring-cloud.version`、`mybatis.version`、`pagehelper.version`、`jjwt.version` 等
- `<dependencyManagement>` 引入 Spring Boot BOM、Spring Cloud BOM、Alibaba BOM
- 引入子模块需要的全部依赖（**不写版本号**，由 BOM 控制）

**步骤 1.3**：在父工程添加构建插件：`maven-compiler-plugin`（`source/target=21`、`-parameters`）、`spring-boot-maven-plugin`（`configuration.skip=true`，因为只有业务模块才需要打可执行 jar）。

### 4.3 关键 pom.xml 结构
```xml
<project>
    <groupId>com.railway</groupId>
    <artifactId>railway-ticket</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <modules>
        <module>common</module>
        <module>gateway</module>
        <module>service-user</module>
        <module>service-train-stock</module>
        <module>service-ticket</module>
        <module>service-payment</module>
        <module>service-order</module>
        <module>service-notification</module>
    </modules>

    <properties>
        <java.version>21</java.version>
        <spring-cloud.version>2025.0.0</spring-cloud.version>
        <spring-cloud-alibaba.version>2025.0.0.0-preview</spring-cloud-alibaba.version>
        <mybatis.version>3.0.3</mybatis.version>
        <pagehelper.version>2.1.0</pagehelper.version>
        <jjwt.version>0.12.6</jjwt.version>
        <hutool.version>5.8.32</hutool.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency><groupId>org.springframework.boot</groupId><artifactId>spring-boot-dependencies</artifactId><version>3.5.14</version><type>pom</type><scope>import</scope></dependency>
            <dependency><groupId>org.springframework.cloud</groupId><artifactId>spring-cloud-dependencies</artifactId><version>${spring-cloud.version}</version><type>pom</type><scope>import</scope></dependency>
            <dependency><groupId>com.alibaba.cloud</groupId><artifactId>spring-cloud-alibaba-dependencies</artifactId><version>${spring-cloud-alibaba.version}</version><type>pom</type><scope>import</scope></dependency>
            <dependency><groupId>org.mybatis.spring.boot</groupId><artifactId>mybatis-spring-boot-starter</artifactId><version>${mybatis.version}</version></dependency>
            <dependency><groupId>com.github.pagehelper</groupId><artifactId>pagehelper-spring-boot-starter</artifactId><version>${pagehelper.version}</version></dependency>
            <dependency><groupId>io.jsonwebtoken</groupId><artifactId>jjwt-api</artifactId><version>${jjwt.version}</version></dependency>
            <dependency><groupId>io.jsonwebtoken</groupId><artifactId>jjwt-impl</artifactId><version>${jjwt.version}</version></dependency>
            <dependency><groupId>io.jsonwebtoken</groupId><artifactId>jjwt-jackson</artifactId><version>${jjwt.version}</version></dependency>
            <dependency><groupId>cn.hutool</groupId><artifactId>hutool-all</artifactId><version>${hutool.version}</version></dependency>
            <dependency><groupId>com.railway</groupId><artifactId>common</artifactId><version>${project.version}</version></dependency>
        </dependencies>
    </dependencyManagement>
</project>
```

### 4.4 验证
```
mvn -N help:effective-pom    # 看到版本被正确解析
mvn validate                  # 父工程能解析
```
此时子模块还未创建，**不要求 install 通过**。

---

## 5. 阶段二：common 公共模块（**先做这个**）

> **为什么先做**：所有业务服务都要用到 R、异常、JWT、雪花、拦截器。任何服务都 `import com.railway.common.*`。

### 5.1 模块结构
```
common
├── pom.xml                # 打包 jar，依赖 jjwt、mybatis-spring-boot-starter、pagehelper-spring-boot-starter、spring-boot-starter-data-redis、spring-boot-starter-aop、spring-boot-starter-validation、hutool
└── src/main/java/com/railway/common/
    ├── config/
    │   ├── JacksonConfig.java
    │   ├── MyBatisConfig.java              # MapperScanner、PageHelper、TypeHandler
    │   ├── RedisConfig.java
    │   ├── RedisLuaConfig.java
    │   └── SecurityConfig.java     # 注册 BCryptPasswordEncoder
    ├── exception/
    │   ├── BizException.java
    │   ├── ErrorCode.java
    │   └── GlobalExceptionHandler.java
    ├── model/
    │   ├── R.java
    │   └── LoginUser.java
    ├── util/
    │   ├── JwtUtil.java
    │   ├── SnowflakeIdWorker.java
    │   ├── IdCardValidator.java
    │   └── UserContext.java
    ├── annotation/
    │   └── RequireRole.java
    ├── aspect/
    │   └── RequireRoleAspect.java
    ├── interceptor/
    │   └── AuthInterceptor.java
    ├── constant/
    │   ├── RedisKey.java
    │   └── HeaderConstant.java
    └── auto/
        └── WebAutoConfiguration.java   # @AutoConfiguration 装配拦截器、AOP
```

### 5.2 关键实现清单

#### 5.2.1 `R<T>` 统一返回体
- 字段：`code`（200 成功 / 业务码）、`message`、`data`、`traceId`。
- 静态方法：`R.ok()`、`R.ok(T data)`、`R.fail(ErrorCode)`、`R.fail(int code, String msg)`。

#### 5.2.2 `ErrorCode` + `BizException` + `GlobalExceptionHandler`
- `ErrorCode` 枚举：`OK(200)`、`BAD_REQUEST(400)`、`UNAUTHORIZED(401)`、`FORBIDDEN(403)`、`NOT_FOUND(404)`、`CONFLICT(409)`、`SERVER_ERROR(500)`、`BIZ_ERROR(1000)`、`STOCK_NOT_ENOUGH(2001)`、`ORDER_EXPIRED(3001)` …
- `BizException` 继承 `RuntimeException`，持有 `ErrorCode`。
- `GlobalExceptionHandler`（`@RestControllerAdvice`）统一处理：
  - `BizException`
  - `MethodArgumentNotValidException`（`@Valid` 校验失败 → 400 + 字段错误信息）
  - `MissingServletRequestParameterException` / `ConstraintViolationException`
  - `FeignException`（转 502）
  - `Exception`（兜底 500，**不暴露堆栈给前端**）

#### 5.2.3 `LoginUser` + `UserContext`
- `LoginUser { Long userId; String username; Set<String> roles; }`
- `UserContext` 用 `ThreadLocal<LoginUser>` 暴露 `set/get/clear`。
- 提供 `currentUserId()`、`mustCurrentUserId()` 便捷方法。

#### 5.2.4 `JwtUtil`
- 依赖：jjwt 0.12.x。
- 配置：`secret`（≥32 字节，从 Nacos 注入）、`expireMinutes=120`、`issuer=railway`。
- 方法：
  - `String generate(LoginUser)`：签发 token，claims = `userId`、`username`、`roles`。
  - `Claims parse(String)`：解析失败抛 `BizException(UNAUTHORIZED)`。
  - `String getTokenFromHeader(String authHeader)`：去掉 `Bearer ` 前缀。

#### 5.2.5 `SnowflakeIdWorker`
- 标准 1bit-41bit-10bit-12bit 实现。
- 通过 `@Value("${snowflake.worker-id:1}")` 注入 `workerId`。
- 暴露静态 `getInstance().nextId()`（双检锁单例）。
- 时钟回拨保护：记录 `lastTimestamp`，回拨则 **自旋 1ms 重试**，超过 5ms 抛异常。

#### 5.2.6 `IdCardValidator`
- 用正则粗校验 18 位 + 校验位 + 生日合法性。
- 暴露 `boolean isValid(String)`、`String mask(String)`（前 6 后 4 中间打码）。

#### 5.2.7 `BCryptPasswordEncoder` Bean
- 在 `SecurityConfig` 中只暴露 `PasswordEncoder` Bean，**不引入 spring-security-web**（避免与 gateway 冲突）。
- 强度 10（默认）。

#### 5.2.8 `AuthInterceptor` + `WebAutoConfiguration`
- `AuthInterceptor implements HandlerInterceptor`：
  1. 读取 `Authorization` 头 → `JwtUtil.parse` → 构造 `LoginUser`。
  2. 写入 `UserContext`，方法结束后 `UserContext.clear()`（`afterCompletion`）。
  3. 解析失败 → 抛 `BizException(UNAUTHORIZED)`。
- 标记 `@AuthIgnore` 注解的 Controller 方法 **跳过** 拦截器（用于登录/注册）。
- `WebAutoConfiguration`：
  ```java
  @AutoConfiguration
  public class WebAutoConfiguration implements WebMvcConfigurer {
      @Bean public AuthInterceptor authInterceptor(){ return new AuthInterceptor(); }
      @Override public void addInterceptors(InterceptorRegistry r) {
          r.addInterceptor(authInterceptor()).addPathPatterns("/**");
      }
  }
  ```
- 在 `META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports` 写入该类全限定名，让各服务 **自动装配**。

#### 5.2.9 `RequireRole` 注解 + `RequireRoleAspect`
- 注解：`@RequireRole({"ADMIN"})`。
- 切面：前置从 `UserContext` 取 `roles`，交集为空则 `BizException(FORBIDDEN)`。

#### 5.2.10 `RedisConfig` + `RedisLuaConfig`
- `RedisConfig`：使用 `GenericJackson2JsonRedisSerializer`，`StringRedisTemplate` 用 `StringRedisSerializer`。
- `RedisLuaConfig`：提供 `DefaultRedisScript<Long>` Bean，加载 `classpath:lua/occupy_stock.lua`、`release_stock.lua`（脚本在 `service-train-stock` 里也保留一份，这里只放通用工具）。
- `RedisKey` 工具类统一生成 key：`STOCK_LOCK:{trainNo}:{date}:{seatType}`、`USER_LOGIN:{userId}`。

#### 5.2.11 `JacksonConfig`
- `JavaTimeModule` 注册；`LocalDateTime` 序列化 `yyyy-MM-dd HH:mm:ss`，时区 `Asia/Shanghai`；`Long` 序列化为字符串防精度丢失。

#### 5.2.12 `MyBatisConfig`
- `@MapperScan("com.railway.**.mapper")` 扫描所有业务模块的 Mapper 接口。
- 注册 `PageInterceptor`（PageHelper 插件），设置 `dialect=mysql`、`reasonable=true`。
- 全局 `Configuration`：`mapUnderscoreToCamelCase=true`、`logImpl=Slf4jImpl`（打印 SQL）。
- 自定义 TypeHandler：`LocalDateTime ↔ Timestamp`、枚举 `String ↔ Enum`。
- `createTime` / `updateTime` 由 Service 层手动 `new Date()` 写入（不依赖 MyBatis-Plus 自动填充）。

### 5.3 common 的 pom.xml 关键依赖
```
spring-boot-starter-web
spring-boot-starter-aop
spring-boot-starter-validation
spring-boot-starter-data-redis
spring-boot-starter-data-redis-reactive(可选)
mybatis-spring-boot-starter
pagehelper-spring-boot-starter
mysql-connector-j
jjwt-api / jjwt-impl / jjwt-jackson
spring-security-crypto          (BCrypt)
hutool-all
lombok(provided)
```

### 5.4 验证
1. `mvn -pl common -am install` 通过。
2. 临时写一个 `main` 方法 `new SnowflakeIdWorker(1).nextId()` 连点 3 次，输出递增。
3. `JwtUtil.generate` / `parse` 往返一次不抛异常。

---

## 6. 阶段三：service-user 用户服务（**最简业务，最先跑通**）

> 选这个先做，理由：① 只依赖 common + MySQL + Redis；② 完整跑通"注册→登录→JWT 鉴权"这条主线，给后续所有服务提供 `userId`。

### 6.1 数据库表（init.sql）
```sql
CREATE TABLE `user` (
  `id` BIGINT PRIMARY KEY,
  `username` VARCHAR(50) UNIQUE NOT NULL,
  `password` VARCHAR(100) NOT NULL,        -- BCrypt
  `phone` VARCHAR(20),
  `email` VARCHAR(100),
  `id_card` VARCHAR(32),                   -- 身份证（可选）
  `real_name` VARCHAR(50),
  `user_type` TINYINT DEFAULT 0,           -- 0 普通 1 管理员
  `status` TINYINT DEFAULT 1,
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

CREATE TABLE `passenger` (
  `id` BIGINT PRIMARY KEY,
  `user_id` BIGINT NOT NULL,
  `name` VARCHAR(50) NOT NULL,
  `id_card_type` TINYINT DEFAULT 1,        -- 1 身份证
  `id_card_no` VARCHAR(32) NOT NULL,
  `phone` VARCHAR(20),
  `passenger_type` TINYINT DEFAULT 0,      -- 0 成人 1 儿童 2 学生
  `is_default` TINYINT DEFAULT 0,
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP,
  INDEX idx_user (`user_id`)
);
```

### 6.2 步骤

**步骤 3.1**：新建 `service-user` 子模块，`pom.xml` 依赖：
```
common
mysql-connector-j
mybatis-spring-boot-starter              (已在 common)
pagehelper-spring-boot-starter           (已在 common)
spring-boot-starter-data-redis          (已在 common)
spring-cloud-starter-alibaba-nacos-discovery
spring-cloud-starter-alibaba-nacos-config
spring-cloud-starter-openfeign
spring-boot-starter-actuator
```

**步骤 3.2**：`application.yml`（**端口 9101**）：
```yaml
server: { port: 9101 }
spring:
  application: { name: service-user }
  cloud:
    nacos:
      discovery: { server-addr: 127.0.0.1:8848 }
      config:    { server-addr: 127.0.0.1:8848, file-extension: yaml }
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/railway-real?useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: root
  data: { redis: { host: 127.0.0.1, port: 6379 } }
mybatis: { mapper-locations: classpath:mapper/*.xml, type-aliases-package: com.railway.user.entity, configuration: { map-underscore-to-camel-case: true, log-impl: org.apache.ibatis.logging.slf4j.Slf4jImpl } }
pagehelper: { helper-dialect: mysql, reasonable: true, support-methods-arguments: true }
jwt: { secret: 0123456789abcdef0123456789abcdef0123456789abcdef, expire-minutes: 120 }
snowflake: { worker-id: 1 }
```

**步骤 3.3**：Nacos 配置中心创建 DataId `service-user.yaml`，把上述内容托管到 Nacos（**生产推荐**，本地可保留 yml）。

**步骤 3.4**：实体 + Mapper
- `UserDO` / `PassengerDO` 为普通 POJO：`private Long id; private LocalDateTime createTime; ...`
- `UserMapper` 接口 + `UserMapper.xml`：基础方法用注解 `@Insert` / `@Select` / `@Update` / `@Delete`；复杂 SQL（多表联查、分页）放 XML。
- 查询示例：`UserMapper.selectById(id)`、`UserMapper.selectByUsername(username)`，`Service` 层做业务封装。
- 列表分页：`PageHelper.startPage(pageNum, pageSize); List<UserDO> list = userMapper.list(query); PageInfo<UserDO> pageInfo = new PageInfo<>(list);`

**步骤 3.5**：`AuthService.register(RegisterDTO)` 流程
1. 校验 username 唯一。
2. `SnowflakeIdWorker.nextId()` 生成 userId。
3. `passwordEncoder.encode(rawPwd)` BCrypt 加密。
4. 写库。
5. 返回 `userId`。

**步骤 3.6**：`AuthService.login(LoginDTO)` 流程
1. `passwordEncoder.matches(raw, dbPwd)`。
2. 签发 JWT（`JwtUtil.generate`）。
3. **写 Redis** `USER_LOGIN:{userId} = token`，`expire = jwtExpireMinutes`（用于**强制下线**）。
4. 返回 `{token, userId, username}`。

**步骤 3.7**：`AuthController`
```java
@PostMapping("/register") @AuthIgnore public R<Long> register(@Valid @RequestBody RegisterDTO dto);
@PostMapping("/login")    @AuthIgnore public R<LoginVO> login(@Valid @RequestBody LoginDTO dto);
@PostMapping("/logout")   public R<Void> logout();        // 删 Redis
@GetMapping ("/me")       public R<LoginUser> me();       // 直接返回 UserContext.currentUser()
```

**步骤 3.8**：`PassengerController`（标准 CRUD，必须登录）
- `POST /passengers` 新增（自动用 `UserContext.mustCurrentUserId()` 绑定）
- `GET /passengers?page=1&size=10` 分页
- `PUT /passengers/{id}`、`DELETE /passengers/{id}`（只能改自己的）
- 身份证走 `IdCardValidator`，可脱敏返回。

**步骤 3.9**：启动类
```java
@SpringBootApplication
@EnableDiscoveryClient
@MapperScan("com.railway.user.mapper")
public class UserApplication { ... }
```

### 6.3 验证
1. `POST /service-user/register` 创建用户。
2. `POST /service-user/login` 拿到 token。
3. `GET /service-user/me` 带 `Authorization: Bearer xxx` 拿到 LoginUser。
4. 乘车人 CRUD 全程正确返回。

---

## 7. 阶段四：gateway 网关

> 放在 user 之后做，因为网关要放行 `/auth/login`、`/auth/register`，需要先知道路径。

### 7.1 步骤

**步骤 4.1**：新建 `gateway` 模块，pom 依赖：
```
spring-cloud-starter-gateway
spring-cloud-starter-alibaba-nacos-discovery
spring-cloud-starter-alibaba-nacos-config
spring-cloud-starter-loadbalancer
hutool-all
lombok
```
**注意**：gateway **不要** 引入 common 里的 spring-web（gateway 基于 webflux，common 是 servlet）；如果 common 用到拦截器/AOP，在 gateway 中不会被加载——这点要在 common 的 `WebAutoConfiguration` 中**用条件装配**排除：
```java
@AutoConfiguration
@ConditionalOnWebApplication(type = ConditionalOnWebApplication.Type.SERVLET)
public class WebAutoConfiguration { ... }
```

**步骤 4.2**：`application.yml`（**端口 9000**）路由表：
```yaml
server: { port: 9000 }
spring:
  application: { name: gateway }
  cloud:
    nacos: { discovery: { server-addr: 127.0.0.1:8848 } }
    gateway:
      discovery: { locator: { enabled: true, lower-case-service-id: true } }
      routes:
        - id: user          uri: lb://service-user        predicates: [Path=/api/user/**]
          filters:    [StripPrefix=2]
        - id: train-stock   uri: lb://service-train-stock predicates: [Path=/api/train/**]
          filters:    [StripPrefix=2, AuthHeader=Authorization]
        - id: ticket        uri: lb://service-ticket      predicates: [Path=/api/ticket/**]
          filters:    [StripPrefix=2]
        - id: order         uri: lb://service-order       predicates: [Path=/api/order/**]
          filters:    [StripPrefix=2]
        - id: payment       uri: lb://service-payment     predicates: [Path=/api/payment/**]
          filters:    [StripPrefix=2]
      globalcors:
        cors-configurations:
          '[/**]': { allowedOriginPatterns: ["*"], allowedMethods: ["*"], allowedHeaders: ["*"], allowCredentials: true }
```

**步骤 4.3**：`AuthGlobalFilter`（GatewayFilter，全局，**order 最高**）
- 放行白名单：`/api/user/auth/login`、`/api/user/auth/register`、支付回调 `/api/payment/callback/**`、所有 `GET` 的车次查询（按需）。
- 非白名单：无 `Authorization` 头 → 返回 401 JSON。
- 有 token：调用 `JwtUtil.parse`（**common 中提取一个纯 JWT 解析工具类，不依赖 Spring Web**），解析成功后将 `X-User-Id`、`X-User-Name`、`X-User-Roles` 三个 header 透传给下游（下游拦截器再读这三个 header → 写 UserContext）。

**步骤 4.4**：`TraceIdFilter`
- 读请求头 `X-Trace-Id`，没有就 `IdUtil.fastUUID()` 生成。
- 写入响应头 + 透传下游。
- 在 `GlobalExceptionHandler` 的 `R` 中也回填 `traceId`。

### 7.2 验证
- `curl http://localhost:9000/api/user/me -H "Authorization: Bearer xxx"` 返回 200 + 用户信息。
- 不带 token 访问受保护接口 → 401。
- `OPTIONS` 跨域预检 200。

---

## 8. 阶段五：service-train-stock（**核心基础数据 + Redis+Lua**）

> 为什么排第 5：① 需要先有 user 体系确认登录态；② 库存是订单链路的最上游，先把"原子预占"打稳，整个下单链才不会超卖。

### 8.1 数据表
```sql
CREATE TABLE `train` (
  `id` BIGINT PRIMARY KEY,
  `train_no` VARCHAR(20) UNIQUE NOT NULL,        -- G1234
  `train_type` VARCHAR(10),                       -- G/D/K
  `start_station` VARCHAR(50),
  `end_station` VARCHAR(50),
  `start_time` DATETIME,
  `end_time` DATETIME,
  `run_days` VARCHAR(20) DEFAULT '1111111',       -- 周一~周日 0/1
  `status` TINYINT DEFAULT 1
);

CREATE TABLE `train_seat_stock` (
  `id` BIGINT PRIMARY KEY,
  `train_no` VARCHAR(20),
  `run_date` DATE,
  `seat_type` VARCHAR(20),                        -- BUSINESS/FIRST/SECOND/STAND
  `total` INT,
  `remain` INT,
  `version` INT DEFAULT 0,                        -- 乐观锁
  UNIQUE KEY uk (train_no, run_date, seat_type)
);

CREATE TABLE `stock_flow` (
  `id` BIGINT PRIMARY KEY,
  `order_no` VARCHAR(32),
  `train_no` VARCHAR(20),
  `run_date` DATE,
  `seat_type` VARCHAR(20),
  `delta` INT,                                    -- 预占 -N / 释放 +N
  `biz_type` TINYINT,                             -- 1 预占 2 确认 3 释放
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP,
  INDEX idx_order (order_no)
);
```

### 8.2 步骤

**步骤 5.1**：模块搭建（依赖同 user + ES starter 可选）。

**步骤 5.2**：`Train` 实体、`TrainService`：
- `addTrain`（`@RequireRole("ADMIN")`）
- `updateTrain`、`deleteTrain`
- `queryTrains(startStation, endStation, runDate)`（**这是后续 ES 同步的数据源**）
- 实体类为普通 POJO（如 `TrainDO`），**不**使用 MyBatis-Plus 注解。
- 乐观锁手动实现：Mapper.xml 中写
  ```xml
  <update id="updateRemainByVersion">
    UPDATE train_seat_stock
    SET remain = remain - #{num}, version = version + 1
    WHERE train_no = #{trainNo} AND run_date = #{runDate}
      AND seat_type = #{seatType} AND version = #{version}
  </update>
  ```
  Service 层先 `SELECT version` 拿到值，再调用 update，**返回 0 行受影响即视为并发冲突** → 抛 `BizException(STOCK_CONFLICT)`。

**步骤 5.3**：`RedisLuaConfig` 加载两份脚本
```java
@Bean public DefaultRedisScript<Long> occupyStockScript() {
    DefaultRedisScript<Long> s = new DefaultRedisScript<>();
    s.setLocation(new ClassPathResource("lua/occupy_stock.lua"));
    s.setResultType(Long.class);
    return s;
}
@Bean public DefaultRedisScript<Long> releaseStockScript() { ... }
```

**步骤 5.4**：Lua 脚本（**核心，原子性由 Redis 单线程 + Lua 保证**）

`occupy_stock.lua`：
```lua
-- KEYS[1] = stock key, e.g. STOCK:trainNo:date:seatType
-- ARGV[1] = num (要预占的数量, 正整数)
-- 返回：1=成功，0=库存不足，-1=key不存在
local stock = tonumber(redis.call('GET', KEYS[1]))
if stock == nil then return -1 end
local need = tonumber(ARGV[1])
if stock < need then return 0 end
redis.call('DECRBY', KEYS[1], need)
return 1
```

`release_stock.lua`（支付失败/取消时回滚）：
```lua
-- KEYS[1] = stock key
-- ARGV[1] = num
local exists = redis.call('EXISTS', KEYS[1])
if exists == 0 then return -1 end
redis.call('INCRBY', KEYS[1], tonumber(ARGV[1]))
return 1
```

**步骤 5.5**：`StockService` 三个核心方法

```java
public R<OccupyResult> occupy(OccupyDTO dto) {
    String key = RedisKey.stockKey(dto.getTrainNo(), dto.getRunDate(), dto.getSeatType());
    // 1. 懒加载：若 key 不存在，从 DB 加载 remain 并 SET 进 Redis（带 NX + 1天过期）
    if (Boolean.FALSE.equals(redis.hasKey(key))) {
        TrainSeatStock s = stockMapper.selectOne(...);
        if (s == null) return R.fail(NOT_FOUND);
        redis.opsForValue().setIfAbsent(key, String.valueOf(s.getRemain()), Duration.ofDays(1));
    }
    // 2. 预占（Lua 原子）
    Long r = redis.execute(occupyStockScript, List.of(key), String.valueOf(dto.getNum()));
    if (r == 0)  return R.fail(STOCK_NOT_ENOUGH);
    if (r == -1) return R.fail(NOT_FOUND);
    // 3. 写流水
    stockFlowMapper.insert(new StockFlow(... .bizType(1) .delta(-dto.getNum())));
    // 4. 异步落库（用线程池即可，简单场景）更新 DB remain = remain - num
    return R.ok(new OccupyResult(stockKey, dto.getNum()));
}

public R<Void> confirm(ConfirmDTO dto) { /* 写流水 bizType=2 delta=-num，不再回滚库存 */ }
public R<Void> release (ReleaseDTO dto)  { /* Lua 释放 + 写流水 bizType=3 delta=+num + 落库 */ }
```

**步骤 5.6**：`StockController`
```java
@PostMapping("/stock/occupy")   public R<OccupyVO> occupy(@RequestBody OccupyDTO dto);
@PostMapping("/stock/confirm")  public R<Void>      confirm(@RequestBody ConfirmDTO dto);
@PostMapping("/stock/release")  public R<Void>      release(@RequestBody ReleaseDTO dto);
```

**步骤 5.7**：`TrainController` 管理员接口
- `POST /trains` `@RequireRole("ADMIN")` 添加车次
- 同一事务里 `INSERT train_seat_stock` 各种座位类型的 `total/remain`

### 8.3 自测
1. `POST /trains` 录入 G1234，设置 BUSINESS 100 / FIRST 200。
2. `occupy` BUSINESS x3 → 多次正常；剩 0 时再调 → 返回 `STOCK_NOT_ENOUGH`。
3. `release` 一次 → 库存回 3。
4. 用 `jmeter` 100 并发预占 1 张，**最终 `remain = 0` 且没有超卖**（这是 Lua 价值体现）。

---

## 9. 阶段六：service-ticket 票务

> 票务相对独立，主要做**票号的生成 + 状态机**。在 order 之前做，是因为 order 编排要调用 `ticket/issue`。

### 9.1 数据表
```sql
CREATE TABLE `ticket` (
  `id` BIGINT PRIMARY KEY,
  `ticket_no` VARCHAR(32) UNIQUE,            -- 业务票号
  `order_no`  VARCHAR(32),
  `train_no`  VARCHAR(20),
  `run_date`  DATE,
  `seat_type` VARCHAR(20),
  `carriage_no` INT,
  `seat_no`    VARCHAR(10),
  `passenger_id` BIGINT,
  `passenger_name` VARCHAR(50),
  `id_card_no`  VARCHAR(32),
  `price` DECIMAL(10,2),
  `status` TINYINT,                            -- 0 待支付 1 已出票 2 已改签 3 已退
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  INDEX idx_order (order_no), INDEX idx_passenger (passenger_id)
);
```

### 9.2 步骤
1. 模块搭建，依赖同 user。
2. `TicketService.issue(IssueDTO)`：
   - 用 `SnowflakeIdWorker` 生成 `ticketNo`（如 `T` + Long）。
   - 选座：从 Redis 缓存 `SEAT_POOL:{trainNo}:{date}:{seatType}`（LinkedList/Set）取一个号。
   - 写库 status=0。
3. `TicketService.confirm(orderNo)`：批量 update status 0→1。
4. `TicketService.cancel(orderNo)`：update status 0→3，同时回写 ES（可选）。

### 9.3 验证
- 内部直接调 `issue`，能拿到 ticketNo。
- 同一订单号 `confirm` 一次，状态变 1。

---

## 10. 阶段七：service-payment 支付

> 解耦其他服务，**只暴露** "发起支付" + "回调" 两个接口，订单服务依赖它。

### 10.1 数据表
```sql
CREATE TABLE `pay_record` (
  `id` BIGINT PRIMARY KEY,
  `pay_no` VARCHAR(32) UNIQUE,                -- 支付单号
  `order_no` VARCHAR(32),
  `amount` DECIMAL(10,2),
  `pay_channel` VARCHAR(20),                   -- ALIPAY / WECHAT / SIM
  `status` TINYINT,                            -- 0 待支付 1 成功 2 失败 3 已关闭
  `paid_time` DATETIME,
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP,
  INDEX idx_order (order_no)
);
```

### 10.2 步骤
1. 接入：开发期使用**模拟支付**（直接返回 `payUrl` 拼接订单号，前端用定时器轮询结果），生产可替换支付宝/微信。
2. `PayService.createPay(orderNo, amount, channel)`：写 pay_record(status=0)，返回 payUrl。
3. `PayService.callback(payNo, success)`：
   - update pay_record status=1/2。
   - 发送 RabbitMQ 消息 `order.paid`（`orderNo`、`payNo`、`success`），由 order 消费。
4. 暴露给网关白名单 `/api/payment/callback/**`，**不要求登录**。

### 10.3 验证
- 内部调用 `createPay`，能拿到 payUrl。
- 手动触发 `callback`，能写流水 + 发 MQ 消息。

---

## 11. 阶段八：service-order 订单（**核心编排**）

> 整个系统的**心脏**：用 OpenFeign 调用 stock/ticket/payment，用 RabbitMQ 做延迟关单，用 Redis 做幂等。

### 11.1 数据表
```sql
CREATE TABLE `orders` (
  `id` BIGINT PRIMARY KEY,
  `order_no` VARCHAR(32) UNIQUE,
  `user_id`  BIGINT,
  `train_no` VARCHAR(20),
  `run_date` DATE,
  `seat_type` VARCHAR(20),
  `num` INT,
  `amount` DECIMAL(10,2),
  `status` TINYINT,                  -- 0 待支付 1 已支付 2 已取消 3 已退款 4 已完成
  `expire_time` DATETIME,            -- 待支付截止时间
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  INDEX idx_user (user_id), INDEX idx_status_expire (status, expire_time)
);
```

### 11.2 步骤

**步骤 8.1**：Feign 客户端（`feign/` 目录）
```java
@FeignClient("service-train-stock")
public interface StockFeignClient {
    @PostMapping("/stock/occupy")  R<OccupyVO> occupy(@RequestBody OccupyDTO dto);
    @PostMapping("/stock/confirm") R<Void>     confirm(@RequestBody ConfirmDTO dto);
    @PostMapping("/stock/release") R<Void>     release(@RequestBody ReleaseDTO dto);
}
// TicketFeignClient / PaymentFeignClient 同理
```
`feign` 包下放 `FallbackFactory`，**降级**全部返回 R.fail(SERVER_ERROR)，避免雪崩。

**步骤 8.2**：幂等
- `Idempotent` 注解 + 切面：取 header `X-Idempotent-Key`，执行前 `SET key value NX EX 60`（Lua：`SETNX + EXPIRE`），失败抛 `BizException(IDEMPOTENT_REPEAT)`。

**步骤 8.3**：下单流程 `OrderService.create(CreateOrderDTO)`
1. 校验 `num ≤ 5`、用户已登录。
2. 幂等键 + 生成 `orderNo`（雪花）。
3. **Feign 调用 `stockFeign.occupy`**（失败直接返回 `STOCK_NOT_ENOUGH`）。
4. 写 `orders` 记录 `status=0`、`expire_time = now + 15min`。
5. **发送 RabbitMQ 延迟消息**（TTL 15min + DLX → 队列 `order.delay.cancel`），用于超时关单。
6. Feign 调用 `paymentFeign.createPay` 获取 payUrl。
7. 返回 `{orderNo, payUrl, expireTime}`。

> ⚠️ **事务边界**：`occupy` 用的是 Redis，不是 DB 事务。`occupy` 成功 + 写 DB 失败 → 走补偿 `release`，由 `try/catch + finally` 兜底。

**步骤 8.4**：RabbitMQ 配置
- `application.yml`：
  ```yaml
  spring:
    rabbitmq: { host: 127.0.0.1, port: 5672, username: guest, password: guest }
  ```
- `RabbitConfig`：
  - 普通交换机 `order.exchange`、路由 `order.paid`、`order.cancel`。
  - **延迟**交换机 `order.delay.exchange`（无消费者） + 队列 `order.delay.queue`（`x-message-ttl=900000`）→ DLX 指向 `order.exchange`，`routing-key=order.cancel`。
  - 模板消息序列化 `Jackson2JsonMessageConverter`。
  - 消费者开启 `acknowledge-mode: manual` + 死信重试 3 次入队告警队列。

**步骤 8.5**：取消订单 `OrderService.cancel(orderNo, reason)`
1. 乐观锁 update status 0→2（若已是 1 则提示"已支付不可取消"）。
2. `stockFeign.release` 回滚库存。
3. `paymentFeign.closePay` 关闭支付单。
4. 发送 `order.cancel` MQ 消息（通知服务消费）。

**步骤 8.6**：支付成功消费 `OrderPaidConsumer`
- 收到 `order.paid`：
  1. update orders status=1。
  2. `stockFeign.confirm`（扣减 DB 库存，把 Redis 的占用落到 DB）。
  3. `ticketFeign.confirm`（票 status 0→1）。
  4. **不主动发通知**——只发送 `order.paid` 的衍生事件，由 notification 监听。

**步骤 8.7**：延迟关单消费者 `OrderDelayCancelConsumer`
- 收到 `order.cancel` 且 orders.status 仍为 0 → 走 `cancel` 流程（关单 = 释放库存）。
- 消息进入 DLQ 后人工兜底。

### 11.3 验证
1. 下单 1 张 → 库存 -1，order status=0。
2. 调用 `paymentFeign.callback` → order status=1，票 status=1，DB 库存 -1。
3. 下单后**不支付**，等 15 分钟 → 自动关单，库存回滚。
4. 同一 `X-Idempotent-Key` 二次下单 → 提示重复。
5. `service-stock` 宕机 → Feign 降级返回 502，订单创建失败不残留。

---

## 12. 阶段九：service-notification 通知

> **最简单**：消费 MQ，发送消息。无上游依赖（只听 order 发的事件）。

### 12.1 步骤
1. 依赖：`spring-boot-starter-amqp`、`spring-boot-starter-mail`、`hutool`、common。
2. `application.yml` 配置 RabbitMQ + 邮件（QQ 企业邮 / 163）。
3. `NotifyService.sendEmail(to, subject, body)`：基于 `JavaMailSender`。
4. `OrderPaidConsumer @RabbitListener("order.paid.queue")`：
   - 解析 `userId/orderNo/amount`。
   - `userFeign.getEmail(userId)` 拿邮箱（用 OpenFeign 反查 user）。
   - `sendEmail("购票成功", ...)`。
5. 失败重试 3 次 → 进 `notify.dlq` → 记录日志告警。

### 12.2 验证
- 启动后发一条 `order.paid` 消息 → 收件箱能看到邮件。

---

## 13. 阶段十：Elasticsearch 检索增强

> 车次/票查询从 MySQL LIKE 升级到 ES。把**搜索型**和**事务型**系统分开。

### 13.1 集成
- service-train-stock / service-ticket 引入 `spring-boot-starter-data-elasticsearch`。
- `application.yml`：`spring.elasticsearch.uris=http://127.0.0.1:9200`。

### 13.2 索引设计
**train_index**（车次）：
- fields: `trainNo`（keyword）、`trainType`、`startStation`（text+keyword）、`endStation`、`startTime`、`endTime`、`price`（nested by seatType）、`runDate`（date）等。
**ticket_index**（票，可选）：用于"我的票"快速过滤。

### 13.3 同步方案
- 同步双写：写 DB 后再 `esTemplate.save`。
- 异步：用 RabbitMQ `train.sync` 事件，notification 改造为多消费者集群。
- 定时对账：`XxlJob` 每日凌晨全量重建（按需）。

### 13.4 接口
- `GET /trains/search?from=北京&to=上海&date=2026-06-10` → ES 查询 → 返回车次+价格。
- `GET /trains/{trainNo}/seats?date=...` → 走 Redis（用 Lua GET 批量拿各座位类型剩余）。

---

## 14. 阶段十一：联调 / 压测 / 上线

### 14.1 启动顺序
1. 中间件：MySQL → Nacos → Redis → RabbitMQ → ES。
2. 业务服务：**service-user → service-train-stock → service-ticket → service-payment → service-order → service-notification → gateway**。
3. 验证 Nacos 服务列表里 7 个服务都已注册。

### 14.2 端到端用例
| 用例 | 步骤 | 预期 |
|---|---|---|
| 注册登录 | `/auth/register` + `/auth/login` | 拿到 token |
| 搜索车次 | `/trains/search` | 返回车次列表 |
| 下单 | `/orders` 携带 token + idempotent-key | 返回 payUrl，库存 -1 |
| 支付回调 | `POST /payment/callback` 模拟成功 | 订单 1、票 1、DB 库存 -1、邮件送达 |
| 取消订单 | `/orders/{no}/cancel` | 订单 2、库存 +1、支付关闭 |
| 超时关单 | 不支付等 15 分钟 | 同上 |
| 超卖压测 | 100 并发 occupy 1 张 | 最终 `remain=0`，无超卖 |

### 14.3 监控
- Actuator + Prometheus + Grafana：JVM、QPS、RT、错误率。
- Sleuth + Zipkin：分布式链路追踪（已通过 TraceId 串联）。
- Sentinel 控制台：热点参数限流（下单入口）。

### 14.4 常见坑
| 问题 | 解决 |
|---|---|
| Long 精度丢失 | JacksonConfig 注册 `NumberSerializer` → 转 string |
| GatewayFilter 与 service 拦截器对 token 重复解析 | 网关只解析+透传 header，业务服务用 `X-User-Id` |
| 雪花 ID 重复 | 部署多实例时 `workerId` 用 `K8s POD_IP % 1024` 注入 |
| Lua 脚本回滚 NPE | 始终判断 EXISTS，缺失则按"幂等成功"处理 |
| 延迟关单消息丢失 | 开启 RabbitMQ Publisher Confirm + 持久化 |
| Feign 调用丢失 Header | 自定义 `RequestInterceptor` 透传 `X-User-Id` |

---

## 15. 关键代码速查

### 15.1 common 自动装配
`common/src/main/resources/META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports`
```
com.railway.common.auto.WebAutoConfiguration
com.railway.common.config.MyBatisConfig
com.railway.common.config.RedisConfig
com.railway.common.config.SecurityConfig
```

### 15.2 gateway 与 common 的 servlet/webflux 隔离
在 `WebAutoConfiguration` 上加：
```java
@ConditionalOnWebApplication(type = ConditionalOnWebApplication.Type.SERVLET)
```
让 gateway 不会因找不到 `DispatcherServlet` 报错。

### 15.3 雪花 workerId 注入（Nacos）
```yaml
snowflake:
  worker-id: ${POD_ID:1}     # K8s 启动时注入
```

### 15.4 Lua 预占 + 释放完整脚本
见 §8.2 第 4 步。

### 15.5 JWT 透传
- gateway：`exchange.getRequest().mutate().header("X-User-Id", id).build()`
- 业务服务：拦截器读 `X-User-Id` 头 → 写 `UserContext`。

---

## 16. 验收清单

- [ ] 7 个模块 `mvn -DskipTests package` 全部通过
- [ ] Nacos 服务列表 7 个服务注册
- [ ] 注册→登录→搜索车次→下单→支付→出票→通知 全链路跑通
- [ ] 100 并发 occupy 不超卖
- [ ] 15 分钟未支付自动关单
- [ ] ES 车次搜索结果正确
- [ ] Gateway 跨域 / TraceId / 白名单 全部生效
- [ ] JWT 过期 / 篡改 → 401
- [ ] 雪花 ID 连续递增无重复
- [ ] BCrypt 相同密码两次加密盐值不同但 `matches` 成功

---

> **下一步行动建议**：**从阶段一 + 阶段二开始**——把父 POM 改造好，把 common 模块先做出来，**`mvn -pl common -am install` 通过后再开始写 user**。其他任何模块都依赖 common，common 没稳，整个工程会陷入"类找不到 → 改半天"的循环。
